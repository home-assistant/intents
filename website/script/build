# Stop on errors
set -e

cd "$(dirname "$0")/.."

rm -rf dist
cp -r public dist

cd ..
python3 -m script.intentfest summary > website/src-11ty/_data/intentSummary.json
cd website

NODE_ENV=production npm exce -- eleventy
