language: hu
intents:
  HassTurnOff:
    data:
      - sentences:
          - "<turn_on> (<area> <name>|<name> [<area>])"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor