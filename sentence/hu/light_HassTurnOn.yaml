language: hu
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> <area> <name> [<light>]"
          - "<turn_on> <name> <light> [<area>]"
        requires_context:
          domain: "light"
      - sentences:
          - "<turn_on> [<minden>] <light> <area>"
          - "<turn_on> <minden> <area>"
          - "<turn_on> <area> <light>"
        slots:
          domain: "light"
        response: "lights_area"