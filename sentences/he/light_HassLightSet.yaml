language: "he"
intents:
  HassLightSet:
    data:
      # brightness
      - sentences:
          - "קבע את בהירות [ה]<name> ל-<brightness>"
          - "קבע את הבהירות של [ה]<name> ל-<brightness>"
          - "קבע ל-<brightness> את הבהירות של <name>"
          - "הגדר את בהירות [ה]<name> ל-<brightness>"
          - "הגדר את הבהירות של [ה]<name> ל-<brightness>"
          - "הגדר ל-<brightness> את הבהירות של <name>"
          - "שנה את בהירות [ה]<name> ל-<brightness>"
          - "שנה את הבהירות של [ה]<name> ל-<brightness>"
          - "שנה ל-<brightness> את הבהירות של <name>"
          - "שינוי את בהירות [ה]<name> ל-<brightness>"
          - "שינוי את הבהירות של [ה]<name> ל-<brightness>"
          - "שינוי ל-<brightness> את הבהירות של <name>"
      - sentences:
          - "קבע [את ה]בהירות ב<area> ל-<brightness>"
          - "שנה [את ה]בהירות ב<area> ל-<brightness>"
          - "הגדר [את ה]בהירות ב<area> ל-<brightness>"
          - "קבע את בהירות ה<area> ל-<brightness>"
          - "קבע את בהירות ה<area> ל-<brightness>"
        slots:
          name: "all"
      # color
      - sentences:
          - "set <name> [color] to {color}"
          - "set [[the] color of] <name> to {color}"
