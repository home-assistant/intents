language: sl
intents:
  HassTurnOn:
    data:
      - sentences:
          - "odvij <name>" # odvij is not optimal verb for open valve - in collision with open from cover # needs fix
        requires_context:
          domain: valve
        slots:
          domain: valve
        response: valve
