language: sl
intents:
  HassTurnOn:
    data:
      - sentences:
          - "(aktiviraj|<vključi>|zaženi) [sceno] <name> [sceno]"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
