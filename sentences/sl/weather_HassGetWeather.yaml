language: sl
intents:
  HassGetWeather:
    data:
      - sentences:
          - "<kakšno_je> vreme [( for| in| at) <name>]"
          #- "<what_is>[ the] weather[( for| in| at) <name>] like"
          #- "<what_is>[ the] <name> weather[ like]"
        requires_context:
          domain: weather
