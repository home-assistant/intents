language: sl
intents:
  HassTurnOn:
    data:
      - sentences:
          - (<vključi>|<odpri>) {name}
          - (<vključi>|<odpri>) {name} [v|na] {area}
        response: default_name
        excludes_context:
          domain:
            - cover
