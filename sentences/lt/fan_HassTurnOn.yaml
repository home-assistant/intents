language: lt
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> [visus] ventiliatori(ų|us) <area>"
          - "<turn_on> [visus] <area> ventiliatori(ų|us)"
        slots:
          domain: fan
          name: all
        response: fans_area
