language: lt
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> <name>"
          - "<turn_on> <area> <name>"
          - "<turn_on> <name> <area>"
        excludes_context:
          domain: cover
