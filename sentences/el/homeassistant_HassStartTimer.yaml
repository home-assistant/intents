---
language: "el"
intents:
  HassStartTimer:
    data:
      - sentences:
          - "χρονόμετρο για <timer_duration>"
          - "χρονόμετρο για <timer_duration> (που λέγεται|που ονομάζεται) {timer_name:name}"
          - "<timer_duration> χρονόμετρο"
          - "<timer_duration> χρονόμετρο για {timer_name:name}"
          - "<timer_set> [ένα|το|το δικό μου] <timer_duration> χρονόμετρο"
          - "<timer_set> [ένα|το|το δικό μου] χρονόμετρο για <timer_duration>"
          - "<timer_set> [ένα|το|το δικό μου] <timer_duration> χρονόμετρο (που λέγεται|που ονομάζεται|για) {timer_name:name}"
          - "<timer_set> [ένα|το|το δικό μου] χρονόμετρο (που λέγεται|που ονομάζεται) {timer_name:name} για <timer_duration>"
          - "<timer_set> [ένα|το|το δικό μου] χρονόμετρο για <timer_duration> (που λέγεται|που ονομάζεται) {timer_name:name}"

      - sentences:
          - "{timer_command:conversation_command} σε <timer_duration>"
          - "σε <timer_duration> {timer_command:conversation_command}"
        response: command
