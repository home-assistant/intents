language: el
intents:
  HassSetVolume:
    data:
      - sentences:
          - "<numeric_value_set> <name> την ένταση <volume> [(τα|τις) εκατό]"
          - "(ανέβασ(ε|μα)|(κατέβασ(ε|μα))) <name> [ένταση(ης)] <volume> [(τα|τις) εκατό]"
          - "(<numeric_value_set> την ένταση (σε|στο) <volume> [(τα|τις) εκατό];[<articles> ]<name>)"
          - "(άλλαξε την ένταση σε <volume> [(τα|τις) εκατό];[<articles> ]<name>)"
        requires_context:
          domain: media_player
