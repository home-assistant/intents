language: el
intents:
  HassSetVolume:
    data:
      - sentences:
          - "ορίστε την ένταση <articles> <name> σε <volume>"
          - "ένταση <articles> <name> <volume>"
        requires_context:
          domain: media_player
