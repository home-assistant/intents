language: "el"
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<open> {name}"
          - "<turn_on> {name}"
        requires_context:
          domain: valve
        slots:
          domain: valve
        response: valve
