language: "el"
intents:
  HassTurnOff:
    data:
      - sentences:
          - "<close> {name}"
          - "<turn_off> {name}"
        requires_context:
          domain: valve
        slots:
          domain: valve
        response: valve
