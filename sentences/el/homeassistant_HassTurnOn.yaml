language: el
intents:
  HassTurnOn:
    data:
      - sentences:
          - <turn_on> <articles> <name>
          - <articles> <name> να <turn_on>
          - <articles> <name> <turn_on>
          - να <turn_on> <articles> <name>
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
            - valve
