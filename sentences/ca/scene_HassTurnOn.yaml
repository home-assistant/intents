language: ca
intents:
  HassTurnOn:
    data:
      - sentences:
          - "(<engega>|activa) (la escena|l'escena) <name>"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
