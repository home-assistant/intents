language: ca
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<engega> [la escena|l'escena] <name>"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
