language: "en"
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn> on [the] (light | lights) in <area>"
          - "<turn> [the] (light | lights) in <area> on"
          - "<turn> on all [the] lights in <area>"
          - "<turn> all [the] lights in <area> on"
        slots:
          domain: "light"
          name: "all"
