language: "en"
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn> on [the] (light | lights) in [the] <area>"
          - "<turn> [the] (light | lights) in [the] <area> on"
          - "<turn> on all [the] lights in [the] <area>"
          - "<turn> all [the] lights in [the] <area> on"
          - "<turn> on [the] <area> (light | lights)"
        slots:
          domain: "light"
