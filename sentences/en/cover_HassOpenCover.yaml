language: "en"
intents:
  HassOpenCover:
    data:
      - sentences:
          - "(open | raise) <name>"
          - "(open | raise) <name> in <area>"
      - sentences:
          - "(open | raise) [the] garage door"
        slots:
          device_class: "garage_door"
      - sentences:
          - "(open | raise) [the] blinds in <area>"
          - "(open | raise) <area> blinds"
        slots:
          device_class:
            - "blind"
            - "curtain"
            - "shutter"
