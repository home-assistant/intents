language: "en"
intents:
  HassOpenCover:
    data:
      - sentences:
          - "open <name>"
          - "open <name> in <area>"
          - "raise <name>"
          - "raise <name> in <area>"
      - sentences:
          - "open [the] garage door"
          - "raise [the] garage door"
        slots:
          device_class: "garage_door"
      - sentences:
          - "open [the] blinds in <area>"
          - "open <area> blinds"
          - "raise [the] blinds in <area>"
          - "raise <area> blinds"
        slots:
          device_class:
            - "blind"
            - "curtain"
            - "shutter"
