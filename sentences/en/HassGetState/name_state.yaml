---
# HassGetState - name_state
# example: is the sliding door open
# slots:
# - {name}
# - {state}

language: "en"

data:
  - sentences:
      - "is [the] {name} {cover_states:state}"
    name_domains:
      - "cover"
    response: "one_yesno"
