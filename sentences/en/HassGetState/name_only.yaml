---
# HassGetState - name_only
# example: what is the value of sensor 1
# slots: {name}

language: "en"

data:
  - sentences:
      - "what is [the [value of [the]]] {name}"
    name_domains:
      - "sensor"
    response: "default"
