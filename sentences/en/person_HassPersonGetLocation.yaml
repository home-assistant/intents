language: "en"
intents:
  HassPersonGetLocation:
    data:
      - sentences:
          - "<where_is> {person}"
          - "have you seen {person}"
          - "<where_did> {person} go"
        requires_context:
          domain: "person"
        response: "location"
