language: en
intents:
  HassTurnOff:
    data:
      - sentences:
          - "<turn> off <name> [light[s]|switch[es]]"
          - "[<turn>] <name> [light[s]|switch[es]] [to] off"
          - "deactivate <name> [light[s]|switch[es]]"
        excludes_context:
          domain:
            - cover
            - scene
            - sensor
            - script
