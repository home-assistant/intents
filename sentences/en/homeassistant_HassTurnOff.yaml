language: en
intents:
  HassTurnOff:
    data:
      - sentences:
          - "<turn> off <name>"
          - "[<turn>] <name> [to] off"
          - "deactivate <name>"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
