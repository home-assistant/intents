language: en
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn> on <name>"
          - "[<turn>] <name> [to] on"
          - "activate <name>"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
