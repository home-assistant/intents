language: en
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn> on <name>"
          - "<turn> <name> [to] on"
        excludes_context:
          domain:
            - cover
            - scene
            - sensor
            - script
