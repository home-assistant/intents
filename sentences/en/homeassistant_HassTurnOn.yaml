language: en
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn> on <name> [light|switch]"
          - "[<turn>] <name> [light|switch] [to] on"
          - "activate <name> [light|switch]"
        excludes_context:
          domain:
            - cover
            - scene
            - sensor
            - script
