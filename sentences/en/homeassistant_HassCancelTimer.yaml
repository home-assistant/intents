---
language: "en"
intents:
  HassCancelTimer:
    data:
      - sentences:
          - "cancel timer"
          - "cancel <timer_start> timer"
          - "cancel {area} timer"
          - "cancel {timer_name:name} timer"
