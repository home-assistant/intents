language: en
intents:
  HassGetState:
    data:
      - sentences:
          - "<is> <name> {cover_states:state} [<in_area_floor>]"
        response: one_yesno
        requires_context:
          domain: cover
        slots:
          domain: cover

      - sentences:
          - "<what_is> <state> of <name> [<in_area_floor>]"
        response: one
        requires_context:
          domain: cover
        slots:
          domain: cover

      - sentences:
          - "<any> {cover_classes:device_class} {cover_states:state} [<in_area_floor>]"
          - "<any> <area_floor> {cover_classes:device_class} {cover_states:state}"
        response: any
        slots:
          domain: cover

      - sentences:
          - "<all> {cover_classes:device_class} {cover_states:state} [<in_area_floor>]"
          - "<all> <area_floor> {cover_classes:device_class} {cover_states:state}"
        response: all
        slots:
          domain: cover

      - sentences:
          - "<which> {cover_classes:device_class} <is> {cover_states:state} [<in_area_floor>]"
          - "<which> <area_floor> {cover_classes:device_class} <is> {cover_states:state}"
        response: which
        slots:
          domain: cover

      - sentences:
          - "<how_many> {cover_classes:device_class} <is> {cover_states:state} [<in_area_floor>]"
          - "<how_many> <area_floor> {cover_classes:device_class} <is> {cover_states:state}"
        response: how_many
        slots:
          domain: cover
