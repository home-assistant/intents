language: en
intents:
  HassMediaUnpause:
    data:
      - sentences:
          - "((unpause|resume);<name>)"
        requires_context:
          domain: media_player
      - sentences:
          - "(unpause|resume)"
        requires_context:
          area:
            slot: true
