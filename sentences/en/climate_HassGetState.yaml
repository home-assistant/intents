language: en
intents:
  HassGetState:
    data:
      # check state (one)
      - sentences:
          - <what_is> <name> <temp>
        requires_context:
          domain: climate
        response: temperature_short

      - sentences:
          - <what_is> [the] <temp> in <area>
        slots:
          domain: climate
        response: temperature_short
