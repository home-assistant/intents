language: "en"
intents:
  HassClimateGetTemperature:
    data:
      - sentences:
          - "<what_is> [the] <temp>"
          - "<what_is> [the] <temp> in <area>"
          - "how (hot | cold) is it [in <area>]"
