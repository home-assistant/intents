language: en
intents:
  HassTurnOn:
    data:
      - sentences:
          - "lock <name>[ in <area>]"
        requires_context:
          domain: lock
        response: lock

      - sentences:
          - "lock[ all] <lockable>[ in] <area>"
          - "lock[ all] <area>[ <lockable>]"
        slots:
          domain: "lock"
          name: "all"
        response: lock
