language: en
intents:
  HassTurnOn:
    data:
      - sentences:
          - "lock [the] <name> [in <area>]"
        requires_context:
          domain: lock
        response: lock

      - sentences:
          - "lock [all] [the] [<lockable>] [in] <area>"
          - "lock [all] <area> [<lockable>]"
        slots:
          domain: "lock"
          name: "all"
        response: lock
