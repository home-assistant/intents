language: en
intents:
  HassTurnOff:
    data:
      - sentences:
          - "unlock <name> [in <area>]"
        requires_context:
          domain: lock
        response: lock

      - sentences:
          - "unlock [all] <lockable> [in] <area>"
          - "unlock [all] <area> [<lockable>]"
        slots:
          domain: "lock"
          name: "all"
        response: lock

      # Continued Conversation: Confirmation
      - sentences:
          - "unlock [all] <lockable> in the house"
        slots:
          domain: "lock"
        follow_with:
          AskForConfimration:
            question: "Are you sure you want to unloack all doors at the same time?"
            allowed_answers:
              - yes:

        response: lock
