language: en
intents:
  HassTurnOff:
    data:
      - sentences:
          - "unlock [the] <name> [in <area>]"
        requires_context:
          domain: lock
        response: lock

      - sentences:
          - "unlock [all] [the] [<lockable>] [in] <area>"
          - "unlock [all] <area> [<lockable>]"
        slots:
          domain: "lock"
          name: "all"
        response: lock
