language: en
intents:
  HassTurnOn:
    data:
      - sentences:
          - "activate <name> [scene]"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
