language: en
intents:
  HassTurnOn:
    data:
      - sentences:
          - "(run|start) <name> [script]"
        requires_context:
          domain: script
        slots:
          domain: script
        response: script
