language: "en"
intents:
  HassListCompleteItem:
    data:
      - sentences:
          - (check|complete|delete|remove) [off] <item> [off] (off|from|on|in) <my_list>
        response: item_completed
        requires_context:
          domain: todo
        expansion_rules:
          my_list: "[my|the] {name} [list]"
          item: "{shopping_list_item:item}"
