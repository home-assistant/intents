---
language: "en"
intents:
  HassPauseTimer:
    data:
      - sentences:
          - "pause timer"
          - "pause <timer_start> timer"
          - "pause {area} timer"
          - "pause {timer_name:name} timer"
