language: "en"
intents:
  HassClimateSetTemperature:
    data:
      - sentences:
          - "(<set>|adjust) <temp> [in <area>] to <temperature>"
          - "(<set>|adjust) <area> <temp> to <temperature>"
