language: "en"
intents:
  HassClimateSetTemperature:
    data:
      - sentences:
          - "set [the] <temp> to <temperature>"
          - "set [the] <temp> in <area> to <temperature>"
          - "set [the] <area> <temp> to <temperature>"
