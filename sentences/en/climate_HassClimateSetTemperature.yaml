language: "en"
intents:
  HassClimateSetTemperature:
    data:
      - sentences:
          - "set <temp> to <temperature>"
          - "set <temp> in <area> to <temperature>"
          - "set <area> <temp> to <temperature>"
          - "change <temp> to <temperature>"
          - "change <temp> in <area> to <temperature>"
          - "change <area> <temp> to <temperature>"
          - "adjust <temp> to <temperature>"
          - "adjust <temp> in <area> to <temperature>"
          - "adjust <area> <temp> to <temperature>"
