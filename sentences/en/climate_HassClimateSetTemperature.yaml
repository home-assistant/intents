language: "en"
intents:
  HassClimateSetTemperature:
    data:
      - sentences:
          - "(set | change | adjust) <temp> to <temperature>"
          - "(set | change | adjust) <temp> in <area> to <temperature>"
          - "(set | change | adjust) <area> <temp> to <temperature>"
