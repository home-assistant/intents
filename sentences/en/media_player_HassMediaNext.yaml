language: en
intents:
  HassMediaNext:
    data:
      - sentences:
          - "next [track|item] [on|for] <name>"
          - "Skip [(to [the] next | (the|this)) [(song|track)]] [on] <name>"
        requires_context:
          domain: media_player
