language: en
intents:
  HassMediaClearPlaylist:
    data:
      - sentences:
          - "((clear|delete);<name>) (queue|playlist)"
          - "(clear|delete) [the ](queue|playlist) on <name>"
        requires_context:
          domain: media_player
