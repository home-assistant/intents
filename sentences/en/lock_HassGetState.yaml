language: en
intents:
  HassGetState:
    data:
      - sentences:
          - "(is|are) <name> [<currently>] [securely] {lock_states:state} [in <area>]"
          - "(is|are) <name> [securely] {lock_states:state} [in <area>] [<currently>]"
        response: one_yesno
        requires_context:
          domain: lock
        slots:
          domain: lock

      - sentences:
          - "((is|are) [there]|do I have) any <lockable> [<currently>] [securely] {lock_states:state} [in <area>]"
          - "((is|are) [there]|do I have) any <lockable> [securely] {lock_states:state} [in <area>] [<currently>]"
          - "((is|are) [there]|do I have) any [<currently>] [securely] {lock_states:state} <lockable> [in <area>]"
          - "((is|are) [there]|do I have) any [securely] {lock_states:state} <lockable> [in <area>] [<currently>]"
        response: any
        slots:
          domain: lock

      - sentences:
          - "are all [the] <lockable> [<currently>] [securely] {lock_states:state} [in <area>]"
          - "are all [the] <lockable> [securely] {lock_states:state} [in <area>] [<currently>]"
        response: all
        slots:
          domain: lock

      - sentences:
          - "(which|what) <lockable> (is|are) [<currently>] [securely] {lock_states:state} [in <area>]"
          - "(which|what) <lockable> (is|are) [securely] {lock_states:state} [in <area>] [<currently>]"
        response: which
        slots:
          domain: lock

      - sentences:
          - "how many <lockable> (is|are) [<currently>] [securely] {lock_states:state} [in <area>]"
          - "how many <lockable> (is|are) [securely] {lock_states:state} [in <area>] [<currently>]"
        response: how_many
        slots:
          domain: lock

      - sentences:
          - "tell me the [current] status of <name> [in <area>] [<currently>]"
        response: one
        requires_context:
          domain: lock
        slots:
          domain: lock
