language: "fi"
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<laita> <päälle> {name}"
          - "[<laita>] {name} <päälle>"
          - "<käynnistä> {name}"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
            - valve
