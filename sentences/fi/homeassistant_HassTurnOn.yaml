language: "fi"
intents:
  HassTurnOn:
    data:
      - sentences:
          - "[<laita>] (<päälle>;{name};[<alue_pääte>])"
          - "[<laita>] <päälle> {name} <alue> {area}"
          - "[<laita>] (<päälle>;[<alue> {area}];{name})"
          - "<käynnistä> ([<alue> {area}];{name})"
          - "<käynnistä> ({name};[<alue_pääte>])"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - sensor
            - valve
            - script

      # Special room specific sentences
      - sentences:
          - "[<laita>] (<päälle>;{name};[<eteinen_pääte>])"
          - "<käynnistä> ([<eteinen_pääte>];{name})"
        slots:
          area: Eteinen
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - sensor
            - valve
            - script

      - sentences:
          - "[<laita>] (<päälle>;{name};[<ullakko_pääte>])"
          - "<käynnistä> ([<ullakko_pääte>];{name})"
        slots:
          area: Ullakko
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - sensor
            - valve
            - script
