language: "fi"
intents:
  HassTurnOff:
    data:
      - sentences:
          - "<laita> <päältä> {name}"
          - "<laita> {name} <päältä>"
          - "<pysäytä> {name}"
        excludes_context:
          domain: cover
