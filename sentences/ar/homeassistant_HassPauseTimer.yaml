---
language: ar
intents:
  HassPauseTimer:
    data:
      - sentences:
          - "<timer_pause> <timer>"
          - "<timer_pause> <timer> <timer_start>"
          - "<timer_pause> <timer> [<in>] <area>"
          - "<timer_pause> <timer> [<called>] [ال]{timer_name:name}"
