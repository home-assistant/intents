language: ar
intents:
  HassTurnOn:
    data:
      - sentences:
          - "[<turn_on>] [<scene>] <name>"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
