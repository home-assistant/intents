language: ar
intents:
  HassLightSet:
    data:
      # brightness
      - sentences:
          - "[<set>] إضاءة <name> [على] <brightness>"
          - "[<set>] <name> [على] إضاءة <brightness>"
          - "[<set>] <name> [على] <brightness>"
      - sentences:
          - "[<set>] [ال] إضاءة [فى] <area> [على] <brightness>"
          - "[<set>] <area> [على] <brightness> إضاءة"
          - "[<set>] <area> [على] <brightness>"
        slots:
          name: "all"
      # color
      - sentences:
          - "[<set>] لون <name> [على] {color}"
          - "[<set>] <name> [على] اللون {color}"
