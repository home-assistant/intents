---
language: "nl"
intents:
  HassStartTimer:
    data:
      - sentences:
          - "[<timer_set>] [een] <timer_duration> timer"
          - "[<timer_set>] [een] timer (van|voor|op) <timer_duration>"
          - "[<timer_set>] [een] <timer_duration> timer <timer_named> {timer_name:name}"
          - "[<timer_set>] [een] {timer_name:name}[ ]timer (van|voor|op) <timer_duration>"
          - "[<timer_set>] [een] timer <timer_named> {timer_name:name} (van|voor|op) <timer_duration>"
          - "[<timer_set>] [een] timer (van|voor|op) <timer_duration> <timer_named> {timer_name:name}"
          # - "[<would>] [een] <timer_duration> timer (zetten|starten|instellen)"
          # - "[<would>] [een] timer (van|voor|op) <timer_duration> (zetten|starten|instellen)"
          # - "[<would>] [een] {timer_name:name}[ ]timer (van|voor|op) <timer_duration> (zetten|starten|instellen)"
          # - "[<would>] [een] <timer_duration> timer <timer_named> {timer_name:name} (zetten|starten|instellen)"
          # - "[<would>] [een] timer <timer_named> {timer_name:name} (van|voor|op) <timer_duration> (zetten|starten|instellen)"
          # - "[<would>] [een] timer (van|voor|op) <timer_duration> <timer_named> {timer_name:name} (zetten|starten|instellen)"
      - sentences:
          - "{timer_command:conversation_command} over <timer_duration>"
          - "[<would>] over <timer_duration> {timer_command:conversation_command}"
        response: command
