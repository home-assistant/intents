---
language: "nl"
intents:
  HassCancelTimer:
    data:
      - sentences:
          - "<timer_cancel> [<my>] timer"
          - "<timer_cancel> [<my>] <timer_start> timer"
          - "<timer_cancel> [<my>] timer van <timer_start>"
          - "<timer_cancel> [<my>] {area}[ ]timer"
          - "<timer_cancel> [<my>] timer [<in>] <area>"
          - "<timer_cancel> [<my>] {timer_name:name}[ ]timer"
          - "<timer_cancel> [<my>] timer <timer_named> {timer_name:name}"
          - "[<would>] [<my>] timer (stoppen|annuleren|[op] stop zetten)"
          - "[<would>] [<my>] <timer_start> timer (stoppen|annuleren|[op] stop zetten)"
          - "[<would>] [<my>] timer van <timer_start> (stoppen|annuleren|[op] stop zetten)"
          - "[<would>] [<my>] {area}[ ]timer (stoppen|annuleren|[op] stop zetten)"
          - "[<would>] [<my>] timer [<in>] <area> (stoppen|annuleren|[op] stop zetten)"
          - "[<would>] [<my>] {timer_name:name}[ ]timer (stoppen|annuleren|[op] stop zetten)"
          - "[<would>] [<my>] timer <timer_named> {timer_name:name} (stoppen|annuleren|[op] stop zetten)"
        expansion_rules:
          my: (de|mijn|m'n)
