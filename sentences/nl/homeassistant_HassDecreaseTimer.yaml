---
language: "nl"
intents:
  HassDecreaseTimer:
    data:
      - sentences:
          - "haal <timer_duration> (af;van [<my>] timer)"
          - "haal <timer_duration> (af;van [<my>] <timer_start> timer)"
          - "haal <timer_duration> (af;van [<my>] timer (van|voor) <timer_start>)"
          - "haal <timer_duration> (af;van [<my>] {area}[ ]timer)"
          - "haal <timer_duration> (af;van [<my>] timer [<in>] <area>)"
          - "haal <timer_duration> (af;van [<my>] {timer_name:name}[ ]timer)"
          - "haal <timer_duration> (af;van [<my>] timer <timer_named> {timer_name:name})"
          - "[<would>] <timer_duration> (afhalen;van [<my>] timer)"
          - "[<would>] <timer_duration> (afhalen;van [<my>] <timer_start> timer)"
          - "[<would>] <timer_duration> (afhalen;van [<my>] timer (van|voor) <timer_start>)"
          - "[<would>] <timer_duration> (afhalen;van [<my>] {area}[ ]timer)"
          - "[<would>] <timer_duration> (afhalen;van [<my>] timer [<in>] <area>)"
          - "[<would>] <timer_duration> (afhalen;van [<my>] {timer_name:name}[ ]timer)"
          - "[<would>] <timer_duration> (afhalen;van [<my>] timer <timer_named> {timer_name:name})"
        expansion_rules:
          my: (de|mijn|m'n)
      - sentences:
          - "verkort [<my>] timer met <timer_duration>"
          - "verkort [<my>] <timer_start> timer met <timer_duration>"
          - "verkort [<my>] timer van <timer_start> met <timer_duration>"
          - "verkort [<my>] {area}[ ]timer met <timer_duration>"
          - "verkort ([<my>] timer;[<in>] <area>) met <timer_duration>"
          - "verkort [<my>] {timer_name:name}[ ]timer met <timer_duration>"
          - "verkort [<my>] timer <timer_named> {timer_name:name} met <timer_duration>"
          - "kort [<my>] timer in met <timer_duration>"
          - "kort [<my>] <timer_start> timer in met <timer_duration>"
          - "kort [<my>] timer van <timer_start> in met <timer_duration>"
          - "kort [<my>] {area}[ ]timer in met <timer_duration>"
          - "kort ([<my>] timer;[<in>] <area>) in met <timer_duration>"
          - "kort [<my>] {timer_name:name}[ ]timer in met <timer_duration>"
          - "kort [<my>] timer (genaamd|in met [de] naam|voor) {timer_name:name} in met <timer_duration>"
          - "[<would>] [<my>] timer met <timer_duration> (verkorten|inkorten)"
          - "[<would>] [<my>] <timer_start> timer met <timer_duration> (verkorten|inkorten)"
          - "[<would>] [<my>] timer van <timer_start> met <timer_duration> (verkorten|inkorten)"
          - "[<would>] [<my>] {area}[ ]timer met <timer_duration> (verkorten|inkorten)"
          - "[<would>] ([<my>] timer;[<in>] <area>) met <timer_duration> (verkorten|inkorten)"
          - "[<would>] [<my>] {timer_name:name}[ ]timer met <timer_duration> (verkorten|inkorten)"
          - "[<would>] [<my>] timer <timer_named> {timer_name:name} met <timer_duration> (verkorten|inkorten)"
        expansion_rules:
          my: (de|mijn|m'n)
