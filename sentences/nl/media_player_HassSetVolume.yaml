language: nl
intents:
  HassSetVolume:
    data:
      - sentences:
          - "<numeric_value_set> <name> volume (naar|op) <volume> [percent]"
          - "zet <name> [volume] (op|naar) <volume> [percent]"
          - "<numeric_value_set> het volume ((naar|op) <volume> [percent];[van |op ]<name>)"
          - "(zet [het ]volume (omhoog|omlaag) ((naar|op) <volume> [percent];[van |op ]<name>)"
        requires_context:
          domain: media_player
