language: nl
intents:
  HassMediaNext:
    data:
      - sentences:
          - "volgende <media_item> [op|voor] <name>"
          - "[ga|zet] <name>[ op | naar][ het| de] volgende <media_item>"
          - "sla[[ het| de] huidige] (<media_item>|dit) op <name> over"
        requires_context:
          domain: media_player
        expansion_rules:
