language: nl
intents:
  HassLightSet:
    data:
      # brightness
      - sentences:
          - "zet <name> op <brigtness> procent"
          - "zet [de] helderheid van <name> op <brightness> [procent]"
          - "verander [de] helderheid van <name> naar <brightness> [procent]"
      - sentences:
          - "zet [de] helderheid in [de][het] <area> op <brightness> [procent]"
          - "zet <area> helderheid op <brightness> [procent]"
        slots:
          name: "all"
      # color
      - sentences:
          - "zet <name> op {color}"
          - "verander [[de] kleur van] <name> naar {color}"
