language: vi
intents:
  HassSetPosition:
    data:
      - sentences:
          - "<set> <name> (ở|tới) (mức|vị trí) <position> "
          - "<set> <name> thành [mức|vị trí] <position> "
          - "<set> vị trí [của] <name> [thành|tới mức|ở mức] <position> "

        requires_context:
          domain:
            - cover
            - valve
