language: vi
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn> [lên] ([<in>] <area> <name>|<name> [[<in>] <area>]) [lên]"
          - "[<turn>] ([<in>] <area> <name>|<name> [[<in>] <area>]) thành bật"
          - "kích hoạt ([<in>] <area> <name>|<name> [[<in>] <area>])"
          - "[<turn>] <name> [[<in>] <area>] [thành|sang] bật [lên]"

        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
            - valve
