language: vi
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn> (<area> <name>|<name> [[trong|của|ở|tại] <area>])"
          - "[<turn>] (<area> <name>|<name> [[trong|của|ở|tại] <area>]) thành bật"
          - "kích hoạt (<area> <name>|<name> [[trong|của|ở|tại] <area>])"
          - "[<turn>] <name> [[trong|của|ở|tại] <area>] [thành|sang] bật"

        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
            - valve
