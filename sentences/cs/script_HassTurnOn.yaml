language: cs
intents:
  HassTurnOn:
    data:
      - sentences:
          - "[<aktivovat>|<spustit>|<zapnout>] [skript] {name}"
        requires_context:
          domain: script
        slots:
          domain: script
        response: script
