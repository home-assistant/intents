language: "cs"
intents:
  HassLightSet:
    data:
      # brightness
      - sentences:
          - "<set> <name> jas [na] <brightness>"
          - "<set> jas <name> [na] <brightness>"
      - sentences:
          - "<set> <area> jas [na] <brightness>"
          - "<set> jas v <area> [na] <brightness>"
        slots:
          name: "all"
      # color
      - sentences:
          - "<set> <name> barvu [na] {color}"
          - "<set> barvu <name> [na] {color}"
