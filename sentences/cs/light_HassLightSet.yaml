language: "cs"
intents:
  HassLightSet:
    data:
      # brightness
      - sentences:
          - "nastav <name> jas [na] <brightness>"
          - "nastav jas <name> [na] <brightness>"
      - sentences:
          - "nastav <area> jas [na] <brightness>"
          - "nastav jas v <area> [na] <brightness>"
        slots:
          name: "all"
      # color
      - sentences:
          - "nastav <name> barvu [na] {color}"
          - "nastav barvu <name> [na] {color}"
