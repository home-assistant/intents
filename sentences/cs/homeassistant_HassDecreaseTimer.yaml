---
language: "cs"
intents:
  HassDecreaseTimer:
    data:
      - sentences:
          - "(odeb(er|rat)|ub(er|rat)) <timer_duration> (z|od) <casovac>"
          - "(odeb(er|rat)|ub(er|rat)) <timer_duration> (z|od) <casovac> na <timer_start>"
          - "(odeb(er|rat)|ub(er|rat)) <timer_duration> (z|od) <casovac> <area>"
          - "(odeb(er|rat)|ub(er|rat)) <timer_duration> (z|od) <casovac> <nazvany> {timer_name:name}"
          - "(odeb(er|rat)|ub(er|rat)) <timer_duration> (z|od) <casovac> {timer_name:name}"
      - sentences:
          - "zkr(ať|átit) <casovac> o <timer_duration>"
          - "zkr(ať|átit) <casovac> na <timer_start> o <timer_duration>"
          - "zkr(ať|átit) <casovac> <area> o <timer_duration>"
          - "zkr(ať|átit) <casovac> <nazvany> {timer_name:name} o <timer_duration>"
          - "zkr(ať|átit) <casovac> {timer_name:name} o <timer_duration>"
