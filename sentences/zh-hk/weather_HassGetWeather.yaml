language: zh-hk
intents:
  HassGetWeather:
    data:
      - sentences:
          - "<what_is>[ the] <weather>[ <like>]"
      - sentences:
          - "<what_is>[ the] <name> <weather>[ <like>]"
        requires_context:
          domain: weather
