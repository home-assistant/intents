language: zh-hk
intents:
  HassGetWeather:
    data:
      - sentences:
          - "<what_is>[ the] <weather> [<like>]"
          - "<what_is>[ the] [<like>] <weather>"
      - sentences:
          - "<what_is>[ the] <name> <weather>[ <like>]"
          - "<what_is>[ the] <name> [ <like>] <weather>"
        requires_context:
          domain: weather
