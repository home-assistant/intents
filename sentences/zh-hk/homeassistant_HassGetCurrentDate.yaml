language: zh-hk
intents:
  HassGetCurrentDate:
    data:
      - sentences:
          #- "(<what_is>|tell me) (todays|today's|the current) date"
          #- "[today](<what_is>|tell me)the date"
          - "(<today>)(<what_is>)(<date>)"
          #- "what is the date"
