language: fr
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<execute> [le script] {name}"
          - "[<execute>] [<le>]{name}"
        requires_context:
          domain: script
        slots:
          domain: script
        response: script
