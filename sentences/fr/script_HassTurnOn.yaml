language: fr
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<active> [le script] {name}"
          - "[<active>] [<le>]{name}"
        requires_context:
          domain: script
        slots:
          domain: script
        response: script
