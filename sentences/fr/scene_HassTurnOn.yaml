language: fr
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<active> [la scène] {name}"
          - "[<active>] [<le>]{name}"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
