language: fr
intents:
  HassVacuumStart:
    data:
      # name
      - sentences:
          - "<demarre> [<le>]{name}"
        requires_context:
          domain: vacuum
      # Floor
      - sentences:
          - "<nettoie> [<le>]{floor}"
      # The whole house (To be done once core supports it)
