language: fr
intents:
  HassMediaNext:
    data:
      - sentences:
          - "<media> suivant[e] sur [<le>]{name}"
          - "<mets> (<le>|au|à la) <media> suivant[e] [sur] [<le>]{name}"
        requires_context:
          domain: media_player
