language: fr
intents:
  HassMediaUnpause:
    data:
      - sentences:
          # Lire sur la TV
          - "<lis> sur [<le>]{name}"
          # Reprend la lecture sur la TV
          - "<reprends> [la lecture] sur [<le>]{name}"
        requires_context:
          domain: media_player
