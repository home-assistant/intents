language: fr
intents:
  HassMediaUnpause:
    data:
      - sentences:
          - "<lis> sur [<le>]{name}"
          - "<reprends> [la lecture] sur [<le>]{name}"
        requires_context:
          domain: media_player
