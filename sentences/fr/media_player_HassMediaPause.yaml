language: fr
intents:
  HassMediaPause:
    data:
      - sentences:
          # Mets la TV sur pause
          - "<mets> [<le>]{name} (sur|en) pause"
          # Mettre en pause la TV
          - "<mets> (sur|en) pause [<le>]{name} "
          # Pause sur la TV
          - "Pause sur [<le>]{name}"
        requires_context:
          domain: media_player
