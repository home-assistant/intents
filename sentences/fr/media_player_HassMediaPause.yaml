language: fr
intents:
  HassMediaPause:
    data:
      - sentences:
          - "<mets> [<le>]{name} sur pause"
          - "Pause sur [<le>]{name}"
        requires_context:
          domain: media_player
