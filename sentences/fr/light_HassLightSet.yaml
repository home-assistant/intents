language: fr
intents:
  HassLightSet:
    data:
      # luminosité
      - sentences:
          - "<regle> <name> sur <brightness>"
          - "<regle> <name> à <brightness>"
          - "<regle> <lumieres> (dans|de|du) <name> à <brightness>"
        slots:
          name: "all"
      - sentences:
          - "<regle> <lumieres> de la lampe [<area>] à <brightness>"
          - "<regle> <lumieres> de la lampe [dans|du|de] [<area>] à <brightness>"
        slots:
          name: "light.bedroom_lamp"
      # couleur
      - sentences:
          - "<regle> [la couleur de] <lumieres> de <area> en {color}"
        slots:
          name: "all"
