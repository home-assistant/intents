language: fr
intents:
  HassLightSet:
    data:
      # brightness
      - sentences:
          - "mets <name> luminosité à <brightness>"
          - "mets [la] luminosité de <name> à <brightness>"
      - sentences:
          - "mets [la] luminosité [de la | du | de | dans] <area> à <brightness>"
          - "set [the] brightness of <area> to <brightness>"
          - "mets <area> à <brightness>"
        slots:
          name: "all"
      # color
      - sentences:
          - "mets [la] <name> en {color}"
          - "mets [[la] couleur de] <name> en {color}"
