language: fr
intents:
  HassTurnOn:
    data:
      - sentences:
          - <allume> [<le>]{name}
          - <allume> [<le>]{name} <dans> [<le>]{area}
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
            - vacuum
            - valve
