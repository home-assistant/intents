language: "fr"
intents:
  HassBroadcast:
    data:
      - sentences:
          - "<annonce> [<partout>] {message}"
          - "<annonce> [<partout>] le message [suivant] {message}"
          - "<annonce> [<partout>] (que |qu') {message}"
