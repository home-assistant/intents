language: pl
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> (wentylator|wiatrak) <area>"
          - "<turn_on> <area> (wentylator|wiatrak)"
        slots:
          domain: fan
          name: all
        response: fan_area

      - sentences:
          - "<turn_on> [<all>] (wentylatory|wiatraki) <area>"
          - "<turn_on> <area> [<all>] (wentylatory|wiatraki)"
        slots:
          domain: fan
          name: all
        response: fans_area

      - sentences:
          - "<turn_on> <all> (wentylatory|wiatraki)"
        slots:
          domain: fan
          name: all
        response: fans_all

      - sentences:
          - "<turn_on> [<in_here>] (wentylator|wiatrak)"
          - "<turn_on> (wentylator|wiatrak) [<in_here>]"
        response: fan_area
        expansion_rules:
          in_here: "(tutaj|w tym pokoju|w tym pomieszczeniu)"
        slots:
          domain: fan
        requires_context:
          area:
            slot: true

      - sentences:
          - "<turn_on> [<in_here>] (wentylatory|wiatraki)"
          - "<turn_on> (wentylatory|wiatraki) [<in_here>]"
        response: fans_area
        expansion_rules:
          in_here: "(tutaj|w tym pokoju|w tym pomieszczeniu)"
        slots:
          domain: fan
        requires_context:
          area:
            slot: true

      - sentences:
          - "<turn_on> [<in_here>] <all> (wentylatory|wiatraki)"
          - "<turn_on> <all> (wentylatory|wiatraki) [<in_here>]"
        response: fans_all
        expansion_rules:
          in_here: "(tutaj|w tym pokoju|w tym pomieszczeniu)"
        slots:
          domain: fan
        requires_context:
          area:
            slot: true
