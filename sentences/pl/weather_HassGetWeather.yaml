language: pl
intents:
  HassGetWeather:
    data:
      - sentences:
          - "<what_is> [<now>] pogoda"
      - sentences:
          - "<what_is> [<now>] pogoda [(w|dla)] <name>"
        requires_context:
          domain: weather
