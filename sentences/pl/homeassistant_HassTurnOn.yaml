language: pl
intents:
  HassTurnOn:
    data:
      - sentences:
          - "(<turn_on>|<turn_on_light>) <name> [<area>]"
          - "[<area>] (<turn_on>|<turn_on_light>) <name>"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
            - light
            - vacuum
            - fan
