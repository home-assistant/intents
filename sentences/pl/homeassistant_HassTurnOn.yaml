language: pl
intents:
  HassTurnOn:
    data:
      - sentences:
          - (<turn_on>|<turn_on_light>) <name>
        response: default
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
