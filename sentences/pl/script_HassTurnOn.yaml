language: pl
intents:
  HassTurnOn:
    data:
      - sentences:
          - "[<activate>|<turn_on>] [skrypt] <name>"
        requires_context:
          domain: script
        slots:
          domain: script
        response: "script"
