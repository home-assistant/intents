language: pl
intents:
  HassTurnOn:
    data:
      - sentences:
          - "[<turn_on>] skrypt <name>"
        requires_context:
          domain: script
        slots:
          domain: script
        response: script
