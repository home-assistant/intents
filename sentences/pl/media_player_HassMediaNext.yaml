language: pl
intents:
  HassMediaNext:
    data:
      - sentences:
          - "<turn_on> następną (piosenkę|<track>) (w|na) <name>"
          - "<turn_on> następny <track> (w|na) <name>"
          - "<turn_on> kolejną (piosenkę|<track>) (w|na) <name>"
          - "<turn_on> kolejny <track> (w|na) <name>"
          - "odtw(órz|arzaj) następną (piosenkę|<track>) (w|na) <name>"
          - "odtw(órz|arzaj) następny <track> (w|na) <name>"
          - "odtw(órz|arzaj) kolejną (piosenkę|<track>) (w|na) <name>"
          - "odtw(órz|arzaj) kolejny <track> (w|na) <name>"
          - "[za]graj następną (piosenkę|<track>) (w|na) <name>"
          - "[za]graj następny <track> (w|na) <name>"
          - "[za]graj kolejną (piosenkę|<track>) (w|na) <name>"
          - "[za]graj kolejny <track> (w|na) <name>"
        expansion_rules:
          track: "utwór|element|ścieżkę"
        requires_context:
          domain: media_player
