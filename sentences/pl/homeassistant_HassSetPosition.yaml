language: pl
intents:
  HassSetPosition:
    data:
      - sentences:
          - "<set> <name> [pozycję|wartość|wysokość] na <position>"
          - "<set> [pozycję|wartość|wysokość] <name> na <position>"
        requires_context:
          domain:
            - cover
            - valve
