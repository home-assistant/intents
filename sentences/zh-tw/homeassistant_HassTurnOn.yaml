language: "zh-tw"
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> [<area>] <name>"
          - "[<let>] [<area>] <name> <turn_on>"
        excludes_context:
          domain: "cover"
