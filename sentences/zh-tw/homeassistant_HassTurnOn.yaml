language: "zh-tw"
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> [<area>] <name>[(<light>|<switch>)]"
          - "[<let>][<area>]<name>[(<light>|<switch>)]<turn_on>"
        excludes_context:
          domain:
            - cover
            - scene
            - sensor
            - script
