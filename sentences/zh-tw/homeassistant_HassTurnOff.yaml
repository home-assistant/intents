language: zh-tw
intents:
  HassTurnOff:
    data:
      - sentences:
          - "<turn_off> [<area>] <name>[(<light>|<switch>)]"
          - "[<let>] [<area>] <name>[(<light>|<switch>)]<turn_off>"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
