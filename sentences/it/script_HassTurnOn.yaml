language: "it"
intents:
  HassTurnOn:
    data:
      - sentences:
          - "[<run>] [<the>] [script] {name}"
        requires_context:
          domain: script
        slots:
          domain: script
        response: script
