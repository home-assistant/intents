language: it
intents:
  HassTurnOn:
    data:
      - sentences:
          - "[<turn_on>] [script ]<name>"
        requires_context:
          domain: script
        slots:
          domain: script
        response: script
