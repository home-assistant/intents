language: "it"
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> [<all>] [<the>] <fan> [(<of>|<in>)]<area>"
          - "<turn_on> [<in>]<area> [<all>] [<the>] <fan>"
        slots:
          domain: fan
          name: "all"
        response: fans_area
