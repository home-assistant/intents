language: it
intents:
  HassTurnOff:
    data:
      - sentences:
          - "<turn_off> [<the>]<name> [<in><area>]"
          - "[<the>]<name> <turn_off> [<in><area>]"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
