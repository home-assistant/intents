language: it
intents:
  HassTurnOff:
    data:
      - sentences:
          - "<turn_off> [<the>] <name>"
      - sentences:
          - "<close> [<the>] <name>"
        response: "cover"
      - sentences:
          - "<close> [<the>] <name> [(<in>|<of>)] <area>"
          - "<close> [<in>] <area> [<the>] <name>"
        response: "cover_area"
