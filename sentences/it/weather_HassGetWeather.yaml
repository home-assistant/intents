language: it
intents:
  HassGetWeather:
    data:
      - sentences:
          - "<what_is>[ the] tempo"
      - sentences:
          - "<what_is>[ the] tempo (a) <name>"
        requires_context:
          domain: weather
