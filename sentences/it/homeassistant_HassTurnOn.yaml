language: it
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> [<the>] <name>"
      - sentences:
          - "<open> [<the>] <name>"
        response: "cover"
      - sentences:
          - "<open> [<the>] <name> [(<in> | <of>)] <area>"
          - "<open> [<in>] <area> [<the>] <name>"
        response: "cover_area"
