language: it
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> [<the>]<name> [<in><area>]"
          - "[<the>]<name> <turn_on> [<in><area>]"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
