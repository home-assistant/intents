language: it
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> <name> [<in><area>]"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
            - vacuum
