language: "it"
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> [<the>] [<light>] {name}"
        requires_context:
          domain: "light"
        slots:
          domain: "light"
        response: "light"

      - sentences:
          - "<turn_on> [<all>] [<the>] <light> [<in>] {area}"
        slots:
          domain: "light"
        response: "lights_area"

      - sentences:
          - "<turn_on> [<all>] [<the> <light>]"
        response: "light_all"
        slots:
          domain: "light"
