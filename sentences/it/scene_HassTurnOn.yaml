language: "it"
intents:
  HassTurnOn:
    data:
      - sentences:
          - "[<run>] [<the>] [scena] {name}"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
