language: it
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> [la] ([scena] <name>)"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
      - sentences:
          - "<turn_on> [la] [scena] <name> [(<of>|<in>)] <area>"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
