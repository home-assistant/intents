language: sk
intents:
  HassTurnOn:
    data:
      - sentences:
          - "[<turn_on_activate>|spust(i|iť)|spusť] [skript] {name}"
        requires_context:
          domain: script
        slots:
          domain: script
        response: script
