language: ro
intents:
  HassPauseTimer:
    data:
      - sentences:
          - "<suspenda_timer>"
        expansion_rules:
          object: "<temporizatorul>[ ([<de> ]<timer_start>|<din_zona>|[(<numit>|pentru) ]{timer_name:name})]"
