language: ml
intents:
  HassClimateSetTemperature:
    data:
      - sentences:
          - "<set> <temp> <temperature> [<to>]"
          - "<temp> <temperature> <to> <set>"
          - "<set> <temp> <area><in> <temperature> [<to>]"
          - "<temperature> <to> <temp> <set>"
