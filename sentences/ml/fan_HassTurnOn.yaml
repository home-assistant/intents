language: ml
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> [<all>] (<fan>|<fans>) [<the>] <area><in>"
          - "<turn_on> <area>[<in>] (<fan>|<fans>)"
          - "<turn> [<all>] <area>[<in>] (<fan>|<fans>) <_on>"
        slots:
          domain: fan
          name: all
