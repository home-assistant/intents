language: ml
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> [<all>] (<light> | <lights>) <area><in>"
          - "<turn_on> [<all>] <area>[<in>] (<light> | <lights>)"
          - "<turn> [<all>] (<light> | <lights>) <area><in> <_on>"
        slots:
          domain: light
