language: es
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<enciende> [escena|escenario] <name>"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
