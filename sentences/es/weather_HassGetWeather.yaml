language: es
intents:
  HassGetWeather:
    data:
      - sentences:
          - "qué tiempo hace"
      - sentences:
          - "qué tiempo hace en <name>"
        requires_context:
          domain: weather
