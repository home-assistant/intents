language: es
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<ejecuta> [script] <name>"
        requires_context:
          domain: script
        slots:
          domain: script
        response: script
