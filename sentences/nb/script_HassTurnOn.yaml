language: nb
intents:
  HassTurnOn:
    data:
      - sentences:
          - "(<aktiver>|<skru_på>) <navn> [skript]"
        requires_context:
          domain: script
        slots:
          domain: script
        response: script
