language: nb
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<aktiver> <navn>[s][modus]"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
