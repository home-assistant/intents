language: nb
intents:
  HassMediaPause:
    data:
      - sentences:
          - "(pause;<navn>)"
          - "sett <navn> [på] pause"
        requires_context:
          domain: media_player
