---
language: "nb"
intents:
  HassStartTimer:
    data:
      - sentences:
          - "<timer_duration> [lang ]<timer>"
          - "<timer> på <timer_duration>"
          - "<timer_duration> <timer> [med navn|som heter] {timer_name:name}"
          - "<timer> for <timer_duration> (med navn|som heter|for) {timer_name:name}"
          - "<timer_set> <timer_duration> <timer>"
          - "<timer_set> <timer> på <timer_duration>"
          - "<timer_set> <timer_duration> <timer> (med navn|som heter|for) {timer_name:name}"
          - "<timer_set> <timer> (med navn|som heter|for) {timer_name:name} for <timer_duration>"
          - "<timer_set> <timer> på <timer_duration> (med navn|som heter|for) {timer_name:name}"
        requires_context:
          area:
            slot: false
      - sentences:
          - "{timer_command:conversation_command} in <timer_duration>"
          - "in <timer_duration> {timer_command:conversation_command}"
        response: command
