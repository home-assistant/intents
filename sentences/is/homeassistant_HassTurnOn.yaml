language: is
intents:
  HassTurnOn:
    data:
      - sentences:
          - "(<turn-on>|<activate>) [á|í] <name><the> [(á|í) <area><the>]"
          - "[(settu|sett þú|setja)] <name><the> [(á|í) <area><the>] í gang"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
