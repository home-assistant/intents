language: "is"
intents:
  HassClimateSetTemperature:
    data:
      - sentences:
          - "(<set>) (<temp>) [(á|í) <area>] (á|í) <temperature>"
          - "(<set>) <area><the><temp> (á|í) <temperature>"
          - "(<set>) <temperature> (á|í) [<area><the>]<temp>"
