language: "is"
intents:
  HassClimateSetTemperature:
    data:
      - sentences:
          - "<set> <area> <temp> í <temperature>"
          - "<temp> <area> [á|í] <temperature>"
          - "[<set>] <temp> [(í|á|inní|inná|inni í|inni á) <area>] (á|í) <temperature>"
          - "[<set>] <area><the>[<temp>] (á|í) <temperature>"
          - "[<set>] <temperature> (í|á|inní|inná|inni í|inni á) [<area><the>]<temp>"
