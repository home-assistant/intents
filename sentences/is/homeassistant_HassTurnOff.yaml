language: is
intents:
  HassTurnOff:
    data:
      - sentences:
          - "(<turn-off>|<deactivate>) [á|í] <name><the> [(á|í) <area><the>]"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
