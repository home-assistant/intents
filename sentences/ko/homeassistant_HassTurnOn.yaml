language: ko
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<name> (열어|열어줘)"
        excludes_context:
          domain:
            - binary_sensor
            - light
            - lock
            - scene
            - script
            - sensor
            - fan
