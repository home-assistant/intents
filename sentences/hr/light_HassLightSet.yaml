language: hr
intents:
  HassLightSet:
    data:
      #brightness
      - sentences:
          - "<podesi> <name> na <brightness>"
          - "<podesi> [osvjetljenje | jačinu] [svjetla | svjetlo] <name> na <brightness> [snage | jačine | svjetline]"
          - "<podesi> [[jačinu | intenzitet] svjetl(a|o) | osvjetljenje] <area> [na] <brightness> [snage | jačine | svjetline]"
        slots:
          domain: "light"
      # color
      - sentences:
          - "<podesi> boju <name> na {color}"
          - "<podesi> <name> [na] {color} [boju]"
          - "<podesi> [boju] svjetl(a|o) [u [prostoriji]] <area> na {color}"
        slots:
          domain: "light"
