language: bg
intents:
  HassLightSet:
    data:
      # brightness
      - sentences:
          - "<set> <lights_brightness> [на] <name> [на | до] <brightness>"
          - "<set> <all> <name> <lights_brightness>[на | до] <brightness> "
      - sentences:
          - "<set> [(на | до)] <brightness> <area>"
          - "<set> <lights_brightness> <area> [на | до] <brightness>"
          - "<set> <area> <lights_brightness> [на | до] <brightness> "
        slots:
          name: "all"
      # color
      - sentences:
          - "<set> [цвета на] <name> [на] {color}"
          - "<set> [на] {color} [цвета|цвят] [на] <name>"
          - "<set> <name> {color} [цвят]"
