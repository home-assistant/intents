language: bg
intents:
  HassLightSet:
    data:
      # brightness
      - sentences:
          - "<set> <lampi_brightness> [на] <name> [на | до] <brightness>"
          - "<set> <all> <name> <lampi_brightness>[на | до] <brightness> "
      - sentences:
          - "<set> [(на | до)] <brightness> <area>"
          - "<set> <lampi_brightness> <area> [на | до] <brightness>"
          - "<set> <area> <lampi_brightness> [на | до] <brightness> "
        slots:
          name: "all"
      # color
      - sentences:
          - "<set> [цвета на] <name> [на] {color}"
          - "<set> [на] {color} [цвета|цвят] [на] <name>"
          - "<set> <name> {color} [цвят]"
