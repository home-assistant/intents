language: bg
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> ламп(ата|ите) в <area>"
          - "<turn_on> осветлението в <area>"
          - "<turn_on> всички[те] лампи в <area>"
        slots:
          domain: light
