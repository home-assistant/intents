language: bg
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> ламп(ата|ите) (в|на) <area>"
          - "<turn_on> осветлението (в|на) <area>"
          - "<turn_on> всички[те] лампи (в|на) <area>"
        slots:
          domain: light
