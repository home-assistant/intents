language: zh-cn
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on>[<area>]{name}"
          - "[<let>][<area>]{name}<turn_on>"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
