language: zh-cn
intents:
  HassGetWeather:
    data:
      - sentences:
          - "<what_is>[ the] <weather>[<like>]"
          - "<name>[的]<weather>[<like>] "
      - sentences:
          - "<what_is>[ the] <name> <weather>[ <like>]"
        requires_context:
          domain: weather
