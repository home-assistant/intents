language: eu
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<piztu> <name> [argia[k]|etengailua[k]]"
          - "<name> [argia[k]|etengailua[k]] piztu"
          - "aktibatu <name> [argia[k]|etengailua[k]]"
        excludes_context:
          domain:
            - cover
            - lock
            - scene
            - script
