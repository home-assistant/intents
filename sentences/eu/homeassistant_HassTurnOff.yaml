language: eu
intents:
  HassTurnOff:
    data:
      - sentences:
          - "<itzali> ({name} [{area}n]|{area}ko {name})"
          - "{name} <itzali> [{area}n]"
          - "{area}ko {name} <itzali>"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
