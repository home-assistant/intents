language: pt-br
intents:
  HassLightSet:
    data:
      # brightness
      #- sentences:
      #    - "<mudar_valor_numerico> [a] luminosidade <no_nome> para <luminosidade>"
      #    - "<mudar_valor_numerico> em <luminosidade> [a] luminosidade <no_nome>"
      #    - "<mudar_valor_numerico> <name> para <luminosidade>"
      #  requires_context:
      #    domain: "light"
      #  response: "brightness"

      #- sentences:
      #    - "<mudar_valor_numerico> [a] luminosidade <na_area> para <luminosidade>"
      #    - "<mudar_valor_numerico> em <luminosidade> [a] luminosidade <na_area>"
      #    - "<mudar_valor_numerico> <area> para <luminosidade>"
      #    - "<mudar_valor_numerico> <area> para <luminosidade> de luminosidade"
      #  slots:
      #    name: "all"
      #  response: "brightness"

      # Max/Min brightness
      #    - sentences:
      #        - "<mudar_valor_numerico> [a] luminosidade <no_nome> [no] {brightness_level:brightness}"
      #        - "<mudar_valor_numerico> [no|em] {brightness_level:brightness} [a] luminosidade <no_nome>"
      #      requires_context:
      #        domain: "light"
      #      response: "brightness"

      #    - sentences:
      #        - "<mudar_valor_numerico> [a] luminosidade <na_area> [no] {brightness_level:brightness}"
      #        - "<mudar_valor_numerico> [no|em] {brightness_level:brightness} [a] luminosidade <na_area>"
      #      slots:
      #        domain: "light"
      #        name: "all"
      #      response: "brightness"

      # color
      - sentences:
          - "<mudar> <nome> [em|para] {color}"
          - "<mudar> [[a] cor [de|d[o|a]]] <nome> [em|para] {color}"
        requires_context:
          domain: "light"
        response: color

      - sentences:
          - "<mudar> [[[a] cor [de|d[o|a]]] (<area> | [todas] [as] luzes <na_area>) [em|para] {color}"
          - "<mudar> (<area> | [todas] [as] luzes <na_area>) [em|para] {color}"
        slots:
          domain: "light"
          name: "all"
        response: color
