language: pt-br
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<liga> [a[s]|o[s]] [luz[es]|interruptor[es]] [do|da|no|na] <name>"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
