language: sv
intents:
  HassGetWeather:
    data:
      - sentences:
          - "hur (är|ser) vädret [ut]"
          - "<vad> väder"
      - sentences:
          - "hur (är|ser) vädret [ut] i {name}"
          - "<vad> väder i {name}"
        requires_context:
          domain: weather
