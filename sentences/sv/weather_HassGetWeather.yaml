language: sv
intents:
  HassGetWeather:
    data:
      - sentences:
          - "hur (är|ser) vädret [ut]"
      - sentences:
          - "hur (är|ser) vädret [ut] i {name}"
        requires_context:
          domain: weather
