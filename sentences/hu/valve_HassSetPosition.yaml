language: hu
intents:
  HassSetPosition:
    data:
      - sentences:
          - "(<numeric_value_set>;<name>;[a ]pozícióját;<position>)"
          - "(<open_close_dev>;<name>;<position>)"
        requires_context:
          domain: valve
