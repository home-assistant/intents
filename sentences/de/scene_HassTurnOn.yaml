language: de
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<aktivieren> [<szene>] <name>"
          - "<aktivieren> <name> <szene>"
          - "[<szene>] <name> (<aktivieren>|<an>)"
          - "<name> <szene> (<aktivieren>|<an>)"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
