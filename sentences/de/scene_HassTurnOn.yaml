language: de
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<aktivieren> [<szene>] <name>"
          - "<aktivieren> [die] <name> <szene>"
          - "[<szene>] <name> (<aktivieren>|<an>)"
          - "[die] <name> <szene> (<aktivieren>|<an>)"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
