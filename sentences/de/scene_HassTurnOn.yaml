language: de
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<aktivieren> [<szene>] <name> [<szene>]"
          - "[<szene>] <name> [<szene>] <aktivieren>"
          - "[<szene>] <name> [<szene>] <an>"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
