language: de
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<aktivieren> [<szene>|die] <name> [<szene>]"
          - "[<szene>|die] <name> [<szene>] (<aktivieren>|<an>)"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
