language: de
intents:
  HassClimateSetTemperature:
    data:
      - sentences:
          - "<setzen>[ <area>] [die ]Temperatur auf <temperature> [ein]"
          - "<setzen> [die ]Temperatur <von_dem> <name> auf <temperature>"
          - "<setzen> [die ]Temperatur <name>[s] auf <temperature>"
