language: de
intents:
  HassClimateSetTemperature:
    data:
      - sentences:
          - "<setzen>[ <area>] [die ]Temperatur auf <temperature>"
          - "<setzen> [die ]Temperatur <von_dem> <name> auf <temperature>"
          - "<setzen> [die ]Temperatur <artikel_bestimmt> <name>[s] auf <temperature>"
