language: de
intents:
  HassClimateSetTemperature:
    data:
      - sentences:
          - "[<setzen>][ <area>] [die ]Temperatur auf <temperature>[ <setzen>]"
          - "[<setzen> ][die ]Temperatur <von_dem> <name> auf <temperature>[ <setzen>]"
          - "[<setzen> ][die ]Temperatur <name>[s] auf <temperature>[ <setzen>]"
