language: "de"
intents:
  HassBroadcast:
    data:
      - sentences:
          - "((gib|gebe) (bekannt|durch)|lass alle (wissen|hören)) {message}"
          - "verkünde[ überall] {message}"
          - "sag[e] überall[ Bescheid]{message}"
          - "gib[ überall] Bescheid {message}"
