language: de
intents:
  HassTurnOn:
    data:
      - sentences:
          - (<schalten>|<machen>) (<area> <name>|<name> [in <area>]) <an>
          - starte (<area> <name>|<name> [in <area>])
          - <aktivieren> (<area> <name>|<name> [in <area>])
          - (<area> <name>|<name> [in <area>]) <aktivieren>
          - (<area> <name>|<name> [in <area>]) <an>[<schalten>|<machen>]
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
