language: de
intents:
  HassLightSet:
    data:
      # brightness
      - sentences:
          - "<setzen> die Helligkeit von <name> auf <brightness>"
        slots:
          name: "all"
      - sentences:
          - "<setzen> die Helligkeit [([(von [dem] | vom)] <lampe> | <alle_lampen>)] [<area>] auf <brightness>"
          - "<setzen> [<area>] die Helligkeit [([(von [dem] | vom)] <lampe> | <alle_lampen>)] auf <brightness>"
      # color
      - sentences:
          - "<setzen> [die Farbe von] <name> auf {color}"
        slots:
          name: "all"
      - sentences:
          - "<setzen> die Farbe [([(von [dem] | vom)] <lampe> | <alle_lampen>)] [<area>] auf {color}"
          - "<setzen> [<area>] die Farbe [([(von [dem] | vom)] <lampe> | <alle_lampen>)] auf {color}"
