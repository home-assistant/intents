language: de
intents:
  HassMediaSearchAndPlay:
    data:
      - sentences:
          - "spiel[e] {search_query}"
        requires_context:
          area:
            slot: true

      - sentences:
          - "spiel[e] {search_query} <area>"

      - sentences:
          - "spiel[e] {search_query} [ (<an>|am)] <name>[ <area>]"
        requires_context:
          domain: "media_player"
