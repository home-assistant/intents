language: de
responses:
  errors:
    no_intent: "Entschuldigung, das habe ich nicht verstanden"
    no_area: "Den Bereich {{ area }} gibt es nicht"
    no_domain: "{{ domain }} gibt es nicht in dem Bereich {{ area }}"
    no_device_class: "{{ device_class }} gibt es nicht in dem Bereich {{ area }}"
    no_entity: "{{ entity }} gibt es nicht"
    handle_error: "Es ist etwas schief gegangen"
lists: {}
expansion_rules:
  name: "(den | die | das) {name}"
  area: "(der | dem | den) {area}"
skip_words:
  - "bitte"
