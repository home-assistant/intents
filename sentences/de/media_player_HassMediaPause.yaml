language: de
intents:
  HassMediaPause:
    data:
      - sentences:
          - "(pausiere|pause) <name>"
          - "halte <name> an"
          - "<name> (pausieren|pause|anhalten|stopp|halt)"
        requires_context:
          domain: media_player
