language: de
intents:
  HassMediaPause:
    data:
      - sentences:
          - "(pausiere|pause) <name>"
          - "halte <name> an"
          - "<name> (pausieren|pause|anhalten|stopp|halt)"
        requires_context:
          domain: media_player
      - sentences:
          - "(pausiere|pause)"
          - "halte an"
          - "(pausieren|pause|anhalten|stopp|halt)"
        requires_context:
          area:
            slot: true
      - sentences:
          - "pausiere [<artikel_bestimmt> |mein |meine ](musik|[fernseh]sendung|gerät[e]) ][ im| in [<artikel>]] <area>"
