language: sl
responses:
  intents:
    HassTurnOn:
      default: "Vklapljam {{ state.domain }}" #default: Naprava {{ slots.name }} je vklopljena
      default_name: "Vklapljam {{ slots.name }}"
      lights_area: "Luč vklopljena" #lights_area: Luči v {{ slots.area }} so vklopljene
      fans_area: "Ventilator vklopljen" #fans_area: Ventilatorji v {{ slots.area }} so vklopljeni
      cover_device_class: "Odpiram {{ slots.device_class }}" #cover_single: "Odpiram {{ slots.name }}" #cover: Senčilo {{ slots.name }} je odprto
      cover_single: "Odpiram {{ slots.name }}" #cover_area: Senčila v {{ slots.area }} so odprta
      lock: "Zakljenjena"
      scene: "Aktivirana"
      script: "Zagnana"
