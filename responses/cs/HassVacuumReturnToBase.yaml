language: cs
responses:
  intents:
    HassVacuumReturnToBase:
      default: >
        {% if slots.name %}
          Vracím {{ slots.name }} do doku
        {% else %}
          Vracím vysavač do doku
        {% endif %}
