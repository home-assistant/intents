language: cs
responses:
  intents:
    HassClimateGetTemperature:
      default: "{{ state.state }} {{ 'stupeň' if round(abs(state.state) == 1 else 'stupně' if round(abs(state.state) in [2,3,4] else 'stupňů' }}"
