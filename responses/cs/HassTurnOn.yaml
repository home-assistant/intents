language: cs
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.name }} bylo zapnuto"
      lights_area: "Světla v {{ slots.area }} byla zapnuta"
      fans_area: "Větráky v {{ slots.area }} byly zapnuty"
      fan: "Větrák {{ slots.name }} byl zapnut"
      light: "Světlo {{ slots.name }} bylo zapnuto"
      cover: "{{ slots.name }} otevřeno"
      cover_area: "{{ slots.area }} otevřeno"
      garage: "{{ slots.name }} otevřena."
