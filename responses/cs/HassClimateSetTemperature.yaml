language: cs
responses:
  intents:
    HassClimateSetTemperature:
      default: "Teplota nastavena na {{ slots.temperature }} {{ 'stupeň' if round(abs(state.state) == 1 else 'stupně' if round(abs(state.state) in [2,3,4] else 'stupňů' }}"
