language: cs
responses:
  intents:
    HassClimateSetTemperature:
      default: 'Teplota nastavena na {{ slots.temperature }} {{ ''stupeň'' if state.state == 1 else ''stupně'' if state.state in [2,3,4] else ''stupňů'' }}'
