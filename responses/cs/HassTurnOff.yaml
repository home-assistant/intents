language: cs
responses:
  intents:
    HassTurnOff:
      default: "{{ slots.name }} vypnuto"
      lights_area: "Světla v {{ slots.name }} byla zhasnuta"
      fans_area: "Větráky v {{ slots.name }} byly vypnuty"
      fan: "Větrák {{ slots.name }} vypnut"
      light: "Světlo {{ slots.name }} bylo vypnuto"
