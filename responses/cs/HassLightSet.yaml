language: cs
responses:
  intents:
    HassLightSet:
      brightness: "Jas {{ slots.name }} nastaven na {{ slots.brightness }}"
      brightness_area: "Jas {{ slots.area }} nastaven na {{ slots.brightness }}"
      color: "Barva {{ slots.name }} nastavena na {{ slots.color }}"
#      color_area: "Barva {{ slots.area }} nastavena na {{ slots.color }}"
