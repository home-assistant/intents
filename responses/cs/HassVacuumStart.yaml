language: cs
responses:
  intents:
    HassVacuumStart:
      default: >
        {% if slots.name %}
          Spouštím {{ slots.name }}
        {% else %}
          Spouštím vysavač
        {% endif %}
