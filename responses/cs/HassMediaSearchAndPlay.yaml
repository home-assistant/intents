language: cs
responses:
  intents:
    HassMediaSearchAndPlay:
      default: |
        {% if slots.media: %}
        Přehrávání začíná
        {% else: %}
        Nenalezen žádný odpovídající obsah
        {% endif %}
