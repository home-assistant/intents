language: pt-br
responses:
  intents:
    HassLightSet:
      brightness: "Brilho de {{ slots.name }} ajustado para {{ slots.brightness }}."
      brightness_area: "Brilho na área {{ slots.area }} ajustado para {{ slots.brightness }}."
      color: "Cor de {{ slots.name }} ajustada para {{ slots.color }}."
      color_area: "Cor na área {{ slots.area }} ajustada para {{ slots.color }}."
