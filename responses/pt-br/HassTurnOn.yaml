language: pt-br
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.name }} ligado."
      lights_area: "Luzes em {{ slots.area }} ligadas."
      fans_area: "Ventiladores em {{ slots.area }} ligados."
      cover: "{{ slots.name }} aberto."
      cover_area: "{{ slots.area }} aberto."
