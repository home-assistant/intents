language: nl
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }} helderheid op {{ slots.brightness }} gezet"
      brightness_area: Helderheid in {{ slots.area }} op {{ slots.brightness }} gezet
      color: "{{ slots.name }} kleur op {{ slots.color }} gezet"
      color_area: Kleur in {{ slots.area }} op {{ slots.color }} gezet
