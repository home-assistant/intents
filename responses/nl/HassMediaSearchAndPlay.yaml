language: nl
responses:
  intents:
    HassMediaSearchAndPlay:
      default: |
        {% if slots.media: %}
        Media wordt afgespeeld
        {% else: %}
        Media niet gevonden
        {% endif %}
