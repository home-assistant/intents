language: nl
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.name }} aangezet"
      lights_area: De lampen in {{ slots.area }} zijn aangezet
      fans_area: De ventilatoren in {{ slots.area }} zijn aangezet
      cover: "{{ slots.name }} geopend"
      cover_area: "{{ slots.area }} geopend"
