language: pl
responses:
  intents:
    HassTurnOn:
      default: Włączono {{ slots.name }}
      lights_area: Włączono lampy w {{ slots.area }}
      fans: Włączono wentylator
      fans_area: Włączono wentylatory w {{ slots.area }}
      cover: Otwarto {{ slots.name }}
      cover_area: Otwarto {{ slots.area }}
