language: is
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }} birtustig breytt í {{ slots.brightness }}"
      brightness_area: "Birtustig í {{ slots.area }} breytt í {{ slots.brightness }}"
      color: "{{ slots.name }} lit breytt í {{ slots.color }}"
      color_area: "Lit í {{ slots.area }} breytt í {{ slots.color }}"
