language: hu
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }} fényereje {{ slots.brightness }}."
      brightness_area: "Fényerő {{ slots.brightness }} itt: {{ slots.area }}."
      color: "{{ slots.name }} színe {{ slots.color }}."
      color_area: "A szín {{ slots.color }} itt: {{ slots.area }}."
