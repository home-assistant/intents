language: sv
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }} ljusstyrka ändrades till {{ slots.brightness }}"
      brightness_area: "Ljusstyrkan i {{ slots.area }} ändrades till {{ slots.brightness }}"
      color: "{{ slots.name }} ändrades till {{ slots.color }}"
      color_area: "Lamporna i {{ slots.area }} ändrades till {{ slots.color }}"
