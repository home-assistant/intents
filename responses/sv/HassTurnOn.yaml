language: sv
responses:
  intents:
    HassTurnOn:
      default: Slog på {{ slots.name }}
      lights_area: Slog på lamporna i {{ slots.area }}
      fans_area: Slog på fläktarna i {{ slots.area }}
      cover: Öppnade {{ slots.name }}
      cover_area: Öppnade {{ slots.area }}
