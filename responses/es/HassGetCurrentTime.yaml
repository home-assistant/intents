language: es
responses:
  intents:
    HassGetCurrentTime:
      default: >
        {% set minute_str = '{0:02d}'.format(slots.time.minute) %}
        {% if slots.time.hour > 12: %}
            {% set hour12 = slots.time_hour - 12 %}
        {% elif slots.time.hour == 0: %}
            {% set hour12 = 12 %}
        {% else: %}
            {% set hour12 = slots.time_hour %}
        {% endif %}
        {% if hour12 == 1 : %}
            Es la
        {% else: %}
           Son las
        {% endif %}
        {{hour12}}
        {% if slots.time.minute > 0 %}
            y {{ minute_str }} minutos
        {% endif %}
        {% if slots.time.hour == 0: %}
            de la medianoche
        {% elif slots.time.hour < 4: %}
            de la madrugada
        {% elif slots.time.hour < 12: %}
            de la mañana
        {% elif slots.time.hour == 12: %}
            del mediodía
        {% elif slots.time.hour < 21: %}
            de la tarde
        {% else: %}
            de la noche
        {% endif %}
