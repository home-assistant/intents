language: es
responses:
  intents:
    HassLightSet:
      brightness: "Brillo de {{ slots.name }} ajustado a {{ slots.brightness }}"
      brightness_area: Brillo en {{ slots.area }} ajustado a {{ slots.brightness }}
      color: "Color de {{ slots.name }} ajustado a {{ slots.color }}"
      color_area: Color en {{ slots.area }} ajustado a {{ slots.color }}
