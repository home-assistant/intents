language: es
responses:
  intents:
    HassTurnOn:
      default: Se ha encendido el dispositivo {{ slots.name }}
      lights_area: Luces en {{ slots.area }} encendidas
      fans_area: Ventiladores en {{ slots.area }} encendidos
      cover: Se ha abierto el dispositivo {{ slots.name }}
      cover_area: Apertura en {{ slots.area }} completada
