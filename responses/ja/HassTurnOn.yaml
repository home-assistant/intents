language: ja
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.name|default('') }}をオンにしました"
      lights_area: "ライトをオンにしました"
      light_all: "すべてのライトをオンにしました"
      script: "スクリプトを実行しました"
