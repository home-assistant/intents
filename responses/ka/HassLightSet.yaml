language: ka
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }}ს განათების დონე დავაყენე {{ slots.brightness }}ზე"
      brightness_area: "განათების დონე {{ slots.area }}ში დავაყენე {{ slots.brightness }}ზე"
      color: "{{ slots.name }}ის ფერი დავაყენე {{ slots.color }}ზე"
      color_area: "{{ slots.area }}ს ფერი დავაყენე {{ slots.color }}ზე"
