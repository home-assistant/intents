language: hr
responses:
  intents:
    HassTurnOn:
      default: "Palim {{ state.domain }}"
      default_name: "Palim {{ slots.name }}"
      lights_area: "Svjetla su upaljena"
      fans_area: "Ventilatori su upaljeni"
      cover_single: "Otvaram {{ slots.name }}"
      cover_device_class: "Otvaram {{ slots.device_class }}"
