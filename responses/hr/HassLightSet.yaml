language: hr
responses:
  intents:
    HassLightSet:
      brightness: Svjetlina je postavljena na {{ slots.brightness }} za svjetlo {{ slots.name }}
      brightness_area: Svjetlina je postavljena na {{ slots.brightness }} za prostoriju {{ slots.area }}
      color: 'Boja je postavljena na {{ slots.color }} za svjetlo {{ slots.name }}'
      color_area: Boja je postavljena na {{ slots.color }} za prostoriju {{ slots.area }}
