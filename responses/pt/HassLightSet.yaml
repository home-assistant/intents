language: pt
responses:
  intents:
    HassLightSet:
      brightness: "Brilho (do|da) {{ slots.name }} alterado para {{ slots.brightness }}"
      brightness_area: Brilho (no|na) {{ slots.area }} alterado para {{ slots.brightness }}
      color: "Cor (do|da) {{ slots.name }} alterada para {{ slots.color }}"
      color_area: Cor (no|na) {{ slots.area }} alterada para {{ slots.color }}
