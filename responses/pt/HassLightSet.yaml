language: pt
responses:
  intents:
    HassLightSet:
      brightness: O brilho para {{ slots.name }} alreado para {{ slots.brightness }}
      brightness_area: O brilho para {{ slots.area }} alterado para {{ slots.brightness }}
      color: A cor para {{ slots.name }} alterado para {{ slots.color }}
      color_area: A cor para {{ slots.area }} alterado para {{ slots.color }}
