language: pt
responses:
  intents:
    HassLightSet:
      brightness: 'Brilho (do|da) {{ slots.name }} definido para {{ slots.brightness }}'
      brightness_area: Brilho (no|na) {{ slots.area }} definido para {{ slots.brightness }}
      color: 'Cor (do|da) {{ slots.name }} definida para {{ slots.color }}'
      color_area: Cor (na|no) {{ slots.area }} defenida para {{ slots.color }}
