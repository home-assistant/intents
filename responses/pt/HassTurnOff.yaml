language: pt
responses:
  intents:
    HassTurnOff:
      default: (Desligado|Desligada) (o|a) {{ slots.name }}
      lights_area: Desligadas as luzes (na|no) {{ slots.area }}
      fans_area: Desligadas as ventoinhas (no|na) {{ slots.area }}
      cover: Fechado (o|a) {{ slots.name }}
      cover_area: Fechado (no|na) {{ slots.area }}
      cover_device_class: Fechado (o|a) {{ slots.device_class }}
