language: pt
responses:
  intents:
    HassTurnOff:
      default: Desligado {{ slots.name }}
      lights_area: Desligada a luz na {{ slots.area }}
      fans_area: Desliga a ventilação na {{ slots.area }}
      cover: Fechado {{ slots.name }}
      cover_area: Fechado {{ slots.area }}
