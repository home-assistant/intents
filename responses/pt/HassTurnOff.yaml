language: pt
responses:
  intents:
    HassTurnOff:
      default: {{ slots.name }} desligado com sucesso
      lights_area: As luzes da {{ slots.area }} desligadas com sucesso
      fans_area: Ventilação da {{ slots.area }} desligada com sucesso
      cover: {{ slots.name }} fechado com sucesso
      cover_area: {{ slots.area }} fechado com sucesso
