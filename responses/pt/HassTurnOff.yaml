language: pt
responses:
  intents:
    HassTurnOff:
      default: Desligado (o|a) {{ slots.name }}
      lights_area: Desligadas as luzes (no|na) {{ slots.area }}
      fans_area: Desligadas as ventoinhas (no|na) {{ slots.area }}
      cover: Closed {{ slots.name }}
      cover_area: Closed {{ slots.area }}
      cover_device_class: Closed {{ slots.device_class }}
