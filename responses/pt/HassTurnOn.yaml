language: pt
responses:
  intents:
    HassTurnOn:
      default: Ligado (o|a) {{ slots.name }}
      lights_area: Ligadas as luzes (no|na) {{ slots.area }}
      fans_area: Ligadas as ventoinhas (no|na) {{ slots.area }}
      cover: Opened {{ slots.name }}
      cover_area: Opened {{ slots.area }}
      cover_device_class: Opened {{ slots.device_class }}
