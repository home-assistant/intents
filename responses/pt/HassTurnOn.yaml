language: pt
responses:
  intents:
    HassTurnOn:
      default: Ligado (a | o) {{ slots.name }}
      lights_area: Ligada a luz (na | no) {{ slots.area }}
      fans_area: Ligada a ventilação (na | no) {{ slots.area }}
      cover: (Aberto | Aberta) (a | o) {{ slots.name }}
      cover_area: (Aberto | Aberta) (no | na) {{ slots.area }}
