language: pt
responses:
  intents:
    HassTurnOn:
      default: Ligado {{ slots.name }}
      lights_area: Ligada a luz na {{ slots.area }}
      fans_area: Ligada a ventilação na {{ slots.area }}
      cover: Aberto a {{ slots.name }}
      cover_area: Aberto na {{ slots.area }}
