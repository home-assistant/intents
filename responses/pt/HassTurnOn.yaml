language: pt
responses:
  intents:
    HassTurnOn:
      default: Turned on {{ slots.name }}
      lights_area: Turned on lights in {{ slots.area }}
      fans_area: Turned on fans in {{ slots.area }}
      cover: Aberto (o|a) {{ slots.name }}
      cover_area: Aberto (no|na) {{ slots.area }}
      cover_device_class: Aberto (os|as) {{ slots.device_class }}
