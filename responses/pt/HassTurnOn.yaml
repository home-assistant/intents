language: pt
responses:
  intents:
    HassTurnOn:
      default: Ligado (o|a) {{ slots.name }}
      lights_area: Ligadas as luzes (no|na) {{ slots.area }}
      fans_area: Ligadas as ventoinhas (no|na) {{ slots.area }}
      cover: Aberto (o|a) {{ slots.name }}
      cover_area: Aberto (no|na) {{ slots.area }}
      cover_device_class: Aberto (o|a) {{ slots.device_class }}
