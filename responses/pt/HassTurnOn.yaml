language: pt
responses:
  intents:
    HassTurnOn:
      default: {{ slots.name }} ligado com sucesso
      lights_area: As luzes da {{ slots.area }} foram ligadas com sucesso
      fans_area: A ventilação da {{ slots.area }} foi ligado com sucesso
      cover: {{ slots.name }} aberto com sucesso
      cover_area: {{ slots.area }} aberto com sucesso
