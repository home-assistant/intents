language: sk
responses:
  intents:
    HassMediaSearchAndPlay:
      default: |
        {% if slots.media: %}
        Prehrávanie sa spúšťa
        {% else: %}
        Nenašiel sa žiaden zodpovedajúci obsah
        {% endif %}
