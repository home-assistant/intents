language: sk
responses:
  intents:
    HassLightSet:
      brightness: "Jas nastavený na {{ slots.brightness }} %"
      brightness_area: "Jas v oblasti nastavený na {{ slots.brightness }} %"
      color: "Farba zmenená na {{ slots.color }}"
      color_area: "Farba v oblasti zmenená na {{ slots.color }}"
