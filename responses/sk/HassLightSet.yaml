language: sk
responses:
  intents:
    HassLightSet:
      brightness: "Jas {{ slots.area }} nastavený na {{ slots.brightness }}"
      brightness_area: "Jas {{ slots.area }} nastavený na {{ slots.brightness }}"
      color: "Farba {{ slots.area }} zmenená na {{ slots.color }}"
      color_area: "Farbe {{ slots.area }} zmenená na {{ slots.color }}"
