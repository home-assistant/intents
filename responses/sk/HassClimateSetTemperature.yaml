language: sk
responses:
  intents:
    HassClimateSetTemperature:
      default: "Teplota nastavena na {{ slots.temperature }} {{ 'stupeň' if state.state | int | abs == 1 else 'stupne' if state.state | int | abs in [2,3,4] else 'stupňov' }}"
