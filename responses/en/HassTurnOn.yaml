language: en
responses:
  intents:
    HassTurnOn:
      default: Turned on {{ state.name }}
      lights_area: "Turned on lights in {{ area.name }}"
      fans_area: "Turned on fans in {{ area.name }}"
