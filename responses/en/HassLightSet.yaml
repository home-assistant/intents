language: en
responses:
  intents:
    HassLightSet:
      brightness: '{{ state.name }} brightness set to {{ intent.brightness.value }}'
      brightness_area: 'Brightness in {{ area.name }} set to {{ intent.brightness.value }}'
      color: '{{ state.name }} color set to {{ intent.color.value }}'
      color_area: 'Color in {{ area.name }} set to {{ intent.color.value }}'
