language: en
responses:
  intents:
    HassGetState:
      default: "{{ slots.name }} is {{ state.state }}"
      any_area: "{{ slots.area }}"
      all_area: "{{ slots.area }}"
      cover_area: "{{ slots.area }}"
      light_area: "{{ slots.area }}"
