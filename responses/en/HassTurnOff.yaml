language: en
responses:
  intents:
    HassTurnOff:
      default: Turned off {{ state.name }}
      lights_area: "Turned off lights in {{ area.name }}"
      fans_area: "Turned off fans in {{ area.name }}"
