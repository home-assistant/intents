language: "en"
responses:
  intents:
    HassShoppingListCompleteItem:
      item_completed: |
        {% if slots.completed_items %}
        Checked off {{ slots.completed_items[0] }}
        {% else %}
        No matching item found on your shopping list.
        {% endif %}
