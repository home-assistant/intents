language: en
responses:
  intents:
    HassMediaSearchAndPlay:
      default: |
        {% if slots.media: %}
        Playing media
        {% else: %}
        Media not found
        {% endif %}
