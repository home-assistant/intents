anguage: en
responses:
  intents:
    HassGetWeather:
      default: "It is now {{ state.attributes.temperature }} and {{ state.state }}"

      forecast: "{{ slots.timeframe | capitalize }} it will be {{ state.attributes.forecast.condition }} with a high of {{ state.attributes.forecast.temperature }} and a low of {{ state.attributes.forecast.templow }}"
