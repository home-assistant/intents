language: el
responses:
  intents:
    HassMediaSearchAndPlay:
      default: |
        {% if slots.search_query: %}
        Παίζει πολυμέσα
        {% else: %}
        Δεν βρέθηκαν πολυμέσα
        {% endif %}
