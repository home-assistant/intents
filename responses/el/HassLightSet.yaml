language: el
responses:
  intents:
    HassLightSet:
      brightness: 'Η φωτεινότητα στο {{ slots.name }} ορίστηκε σε {{ slots.brightness }}'
      brightness_area: Η φψτεινότητα στην περιοχή {{ slots.area }} ορίστηκε σε {{ slots.brightness }}
      color: 'Το χρώμα στο {{ slots.name }} ορίστηκε σε {{ slots.color }}'
      color_area: 'Το χρώμα στην περιοχή {{ slots.area }} ορίστηκε σε {{ slots.color }}'
