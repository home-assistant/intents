language: it
responses:
  intents:
    HassTurnOn:
      default: Acceso {{ slots.name }}
      lights_area: Luc(e|i) acces(a|e) in {{ slots.area }}
      fans_area: <fan> acces(o|i) in {{ slots.area }}
      cover: Aperto {{ slots.name }}
      cover_area: Aperto {{ slots.area }}
