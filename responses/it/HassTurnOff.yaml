language: it
responses:
  intents:
    HassTurnOff:
      default: spento {{ slots.name }}
      lights_area: luc(e | i) spent(a | e) in {{ slots.name }}
      fans_area: Spento <fan> in {{ slots.name }}
      cover: chiuso {{ slots.name }}
      cover_area: chiuso {{ slots.area }}
