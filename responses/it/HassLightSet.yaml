language: it
responses:
  intents:
    HassLightSet:
      brightness: '{{ slots.name }} luminosità impostata al {{ slots.brightness }} percento'
      brightness_area: '[la] luminosità in {{ slots.area }} è modificata al {{ slots.brightness }} percento'
      color: '{{ slots.name }} colore modificato in {{ slots.color }}'
      color_area: '[il] colore in {{ slots.area }} è modificato in {{ slots.color }}'
