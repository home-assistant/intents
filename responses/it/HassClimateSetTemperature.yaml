language: "it"
responses:
  intents:
    HassClimateSetTemperature:
      default: "Ho impostato la temperatura a {{ slots.temperature }} {{ 'grado' if (slots.temperature | float) == 1 else 'gradi' }}"
