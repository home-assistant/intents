language: zh-tw
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.name }}已打開"
      lights_area: "{{ slots.area }}燈已打開"
      fans_area: "{{ slots.area }}風扇已打開"
      cover: "{{ slots.name }}已打開"
      cover_area: "{{ slots.area }}窗簾已打開"
      switch: "{{ slots.name }}已打開"
      switch_area: "{{ slots.area }}開關已打開"
      climate: "{{ slots.name }}已打開"
      climate_area: "{{ slots.area }}溫控器已打開"
      cover_door: "{{ slots.area }}門已打開"
      cover_window: "{{ slots.area }}窗戶已打開"
      cover_garage: "{{ slots.area }}車庫門已打開"
      cover_device_class: "{{ slots.device_class }}已打開"
