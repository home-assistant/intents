language: zh-tw
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.name }}已打開"
      lights_area: "{{ slots.area }}照明已開啟"
      fans_area: "{{ slots.area }}已打開"
      cover: "{{ slots.name }}已打開"
      cover_area: "{{ slots.area }}已打開"
      cover_door: "{{ slots.area }}已打開"
      cover_window: "{{ slots.area }}已打開"
