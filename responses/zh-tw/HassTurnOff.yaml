language: zh-tw
responses:
  intents:
    HassTurnOff:
      default: "{{ slots.name }}已關閉"
      lights_area: "{{ slots.area }}已關閉"
      fans_area: "{{ slots.area }}已關閉"
      cover: "{{ slots.area }}{{ slots.name }}已關閉"
      cover_area: "{{ slots.area }}{{ slots.name }}已關閉"
      cover_door: "{{ slots.area }}已關閉"
      cover_window: "{{ slots.area }}已關閉"
