language: zh-tw
responses:
  intents:
    HassTurnOff:
      default: "{{ state.domain }}已關閉"
      lights_area: "燈光已關閉"
      fans_area: "風扇已關閉"
      cover: "已關閉"
      cover_device_class: "{{ slots.device_class }}已關閉"
