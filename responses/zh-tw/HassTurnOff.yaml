language: zh-tw
responses:
  intents:
    HassTurnOff:
      default: "{{ slots.name }}已關閉"
      lights_area: "{{ slots.area }}照明已關閉"
      fans_area: "{{ slots.area }}風扇已關閉"
      cover: "{{ slots.name }}已關閉"
      cover_area: "{{ slots.area }}窗簾已關閉"
      cover_door: "{{ slots.area }}已關閉"
      cover_window: "{{ slots.area }}已關閉"
