language: zh-tw
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }}亮度已設置為{{ slots.brightness }}"
      brightness_area: "{{ slots.area }}亮度已設置為{{ slots.brightness }}"
      color: "{{ slots.name }}顏色已設置為{{ slots.color }}"
      color_area: "{{ slots.area }}顏色已設置為{{ slots.color }}"
