language: zh-tw
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }}亮度已調整為{{ slots.brightness }}"
      brightness_area: "{{ slots.area }}照明亮度已調整為{{ slots.brightness }}"
      color: "{{ slots.name }}顏色已調整為{{ slots.color }}"
      color_area: "{{ slots.area }}照明顏色已調整為{{ slots.color }}"
