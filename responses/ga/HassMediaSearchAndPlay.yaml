language: ga
responses:
  intents:
    HassMediaSearchAndPlay:
      default: "TODO: {% if slots.media: %}

        Playing media

        {% else: %}

        Media not found

        {% endif %}

        "
