language: bg
responses:
  intents:
    HassLightSet:
      brightness: 'Яркостта на {{ slots.name }} е настроен на {{ slots.brightness }}'
      brightness_area: Яркостта в {{ slots.area }} е настроена на {{ slots.brightness }}
      color: 'Цветат на {{ slots.name }} е зададен на {{ slots.color }}'
      color_area: Цветът в {{ slots.area }} е зададен на {{ slots.color }}
