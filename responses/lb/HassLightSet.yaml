language: lb
responses:
  intents:
    HassLightSet:
      brightness: '{{ slots.name }} Hellegkeet ass {{ slots.brightness }}'
      brightness_area: D'Hellegkeet am {{ slots.area }} ass {{ slots.brightness }}
      color: '{{ slots.name }} Faarw ass {{ slots.color }}'
      color_area: D'Faarw vu {{ slots.area }} ass {{ slots.color }}
