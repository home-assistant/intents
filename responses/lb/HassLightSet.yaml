language: lb
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }} Hellegkeet ass elo {{ slots.brightness }}"
      brightness_area: "D'Hellegkeet am {{ slots.area }} ass elo {{ slots.brightness }}"
      color: "{{ slots.name }} Faarw ass elo {{ slots.color }}"
      color_area: "D'Faarw am {{ slots.area }} ass elo {{ slots.color }}"
