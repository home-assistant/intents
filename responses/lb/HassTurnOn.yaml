language: lb
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.name }} ass un"
      lights_area: "D'Luuten am {{ slots.area }} sinn un"
      fans_area: "D'Ventilatioun am {{ slots.area }} ass un"
      cover: "{{ slots.name }} ass [er]op"
      cover_area: "{{ slots.area }} ass [er]op"
