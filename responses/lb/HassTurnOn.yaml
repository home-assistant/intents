language: lb
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.name }} ageschalt"
      lights_area: "D'Luuten am {{ slots.area }} sinn ugeschalt"
      fans_area: "D'Ventilatioun am  {{ slots.area }} ass ugeschalt"
      cover: "{{ slots.name }} ass op"
      cover_area: "{{ slots.area }} ass op"
