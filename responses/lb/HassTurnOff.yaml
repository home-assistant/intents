language: lb
responses:
  intents:
    HassTurnOff:
      default: "{{ slots.name }} ausgeschalt"
      lights_area: "D'Luuten an {{ slots.area }} sinn ausgeschalt"
      fans_area: "D'Ventilatioun am {{ slots.area }} ass ausgeschalt"
      cover: "{{ slots.name }} ass zou"
      cover_area: "{{ slots.area }} ass zou"
