language: uk
responses:
  intents:
    HassTurnOff:
      default:  "{{ slots.name }} вимкнено"
      lights_area: "Світло в {{ slots.name }} вимкнено"
      fans_area: "Вентилятори в {{ slots.name }} вимкнено"
      cover: "{{ slots.name }} закрито"
      cover_area: "{{ slots.area }} відкрито"
