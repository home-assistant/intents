language: de
responses:
  intents:
    HassMediaSearchAndPlay:
      default: |
        {% if slots.media: %}
        Medien werden abgespielt
        {% else: %}
        Medien nicht gefunden
        {% endif %}
