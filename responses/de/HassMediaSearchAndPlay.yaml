language: de
responses:
  intents:
    HassMediaSearchAndPlay:
      default: |
        {% if slots.media: %}
        Wiedergabe wird gestartet
        {% else: %}
        Keine passenden Inhalte gefunden
        {% endif %}
