language: da
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }} lysstyrke sat til {{ slots.brightness }}"
      brightness_area: "lysstyrke i {{ slots.area }} sat til {{ slots.brightness }}"
      color: "{{ slots.name }} farve sat til {{ slots.color }}"
      color_area: "Farve i {{ slots.area }} sat til {{ slots.color }}"
