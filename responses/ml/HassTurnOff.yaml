language: ml
responses:
  intents:
    HassTurnOff:
      default: "{{ slots.name }} നിറുത്തി"
      lights_area: "{{ slots.area }}യിലെ ലൈറ്റുകൾ ഓഫാക്കി"
      fans_area: "{{ slots.area }}യിലെ പങ്കകൾ നിറുത്തി"
      cover: "{{ slots.name }} അടച്ചു"
      cover_area: "{{ slots.area }} അടച്ചു"
      cover_device_class: "{{ slots.device_class }} അടച്ചു"
