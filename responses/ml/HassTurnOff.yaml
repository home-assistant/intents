language: ml
responses:
  intents:
    HassTurnOff:
      default: "{{ slots.name }} നിറുത്തി"
      lights_area: "{{ slots.name }}യിലെ ലൈറ്റുകൾ ഓഫാക്കി"
      fans_area: "{{ slots.name }}യിലെ പങ്കകൾ നിറുത്തി"
      cover: "{{ slots.name }} അടച്ചു"
      cover_area: "{{ slots.area }} അടച്ചു"
