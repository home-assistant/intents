language: tr
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.name }} açıldı"
      lights_area: "{{ slots.area }} alanındaki ışıklar açıldı"
      fans_area: "{{ slots.area }} alanındaki fanlar açıldı"
      cover: "{{ slots.name }} açıldı"
      cover_area: "{{ slots.area }} açıldı"
      cover_device_class: "{{ slots.device_class }} açıldı"
