language: tr
responses:
  intents:
    HassTurnOn:
      default: {{ slots.name }} Açık
      lights_area: {{ slots.area }} ışıkları açtı
      fans_area: {{ slots.area }} Fan açtı
      cover: {{ slots.name }} Açıldı
      cover_area: {{ slots.area }} Açıldı
