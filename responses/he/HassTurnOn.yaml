language: he
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.domain }}ה מדליק את"
      light: "מדליק את האור"
      lights_area: "מדליק את כל האורות ב{{ slots.area }}"
      fans_area: המאווררים ב{{ slots.area }} הופעלו
      cover: פותח {{ slots.name }}
      cover_area: "{{ slots.area }} נפתח"
      cover_device_class: "{{ slots.device_class }} נפתח"
