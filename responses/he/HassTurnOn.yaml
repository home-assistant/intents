language: he
responses:
  intents:
    HassTurnOn:
      default: הפעל {{ slots.name }}
      lights_area: מדליק אורות ב {{ slots.area }}
      fans_area: מפעיל מאווררים ב {{ slots.area }}
      cover: פותח {{ slots.name }}
      cover_area: סוגר {{ slots.area }}
