language: he
responses:
  intents:
    HassTurnOff:
      default: "{{ slots.domain }}מכבה את ה"
      light: "מכבה את האור"
      lights_area: "מכבה את כל האורות ב{{ slots.area }}"
      fans_area: המאווררים ב{{ slots.area }} כובו
      cover: סוגר {{ slots.name }}
      cover_area: "{{ slots.area }} נסגר"
      cover_device_class: "{{ slots.device_class }} נסגר"
