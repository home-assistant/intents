language: he
responses:
  intents:
    HassTurnOff:
      default: "{{ slots.name }} כובה"
      lights_area: "האורות ב{{ slots.area }} כובו"
      fans_area: "המאווררים ב{{ slots.area }} כובו"
      cover: "{{ slots.name }} נסגר"
      cover_area: "{{ slots.area }} נסגר"
      cover_device_class: "{{ slots.device_class }} נסגר"
