language: kw
responses:
  intents:
    HassClimateSetTemperature:
      default: "TODO: Temperature set to {{ slots.temperature }} {{ 'degree' if
        (slots.temperature | float) == 1 else 'degrees' }}"
