language: ro
responses:
  intents:
    HassOpenCover:
      default: "Am deschis {{ slots.name }}"
      covers_area: "Am deschis în {{ slots.area }}"
      doors_area: "Am deschis ușa de la {{ slots.area }}"
      windows_area: "Am deschis fereastra de la {{ slots.area }}"
      gates_area: "Am deschis poarta de la {{ slots.area }}"
