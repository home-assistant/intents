language: ro
responses:
  intents:
    HassTurnOff:
      default: "Am oprit {{ slots.name }}"
      lights_area: "Am oprit luminile din {{ slots.name }}"
      fans_area: "Am oprit ventilatoarele din {{ slots.name }}"
