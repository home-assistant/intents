language: ro
responses:
  intents:
    HassCloseCover:
      default: "Am închis {{ slots.name }}"
      covers_area: "Am acoperit în {{ slots.area }}"
      doors_area: "Am închis ușa de la {{ slots.area }}"
      windows_area: "Am închis fereastra de la {{ slots.area }}"
      gates_area: "Am închis poarta de la {{ slots.area }}"
