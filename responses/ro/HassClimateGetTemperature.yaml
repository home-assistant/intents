language: ro
responses:
  intents:
    HassClimateGetTemperature:
      success: "{{ state.state }} {{ 'grad' if state.state | float(0) | abs == 1 else ('grade' if state.state | float(0) % 1 != 0 or state.state | float(99) | abs < 20 or state.state | float(0) % 100 | abs < 20  else 'de grade') }}"
