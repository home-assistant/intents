language: vi
responses:
  intents:
    HassTurnOn:
      default: Đã bật {{ slots.name }}
      lights_area: Đã bật đèn trong {{ slots.area }}
      fans_area: Đã bật quạt trong {{ slots.area }}
      cover: Đã mở {{ slots.name }}
      cover_area: Đã mở toàn bộ rèm {{ slots.area }}
