language: zh-cn
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }}亮度已經設定為{{ slots.brightness }}"
      brightness_area: "{{ slots.area }}的照明亮度已經設定為{{ slots.brightness }}"
      color: "{{ slots.name }}色溫已經設定為{{ slots.color }}"
      color_area: "{{ slots.area }}的照明色溫已經設定為{{ slots.color }}"
