language: zh-cn
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }} 亮度设置为 {{ slots.brightness }}"
      brightness_area: "{{ slots.area }} 灯光亮度设置为 {{ slots.brightness }}"
      color: "{{ slots.name }} 颜色设置为 {{ slots.color }}"
      color_area: "{{ slots.area }} 灯光颜色设置为 {{ slots.color }}"
