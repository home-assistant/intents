language: zh-cn
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.name }} 已打开"
      lights_area: "{{ slots.area }} 灯已打开"
      fans_area: "{{ slots.area }} 风扇已打开"
      cover: "{{ slots.name }} 已打开"
      cover_area: "{{ slots.area }} 窗帘已打开"
