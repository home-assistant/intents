language: zh-cn
responses:
  intents:
    HassTurnOff:
      default: "{{ slots.name }}已关闭"
      lights_area: "{{ slots.area }}灯已关闭"
      fans_area: "{{ slots.area }}风扇已关闭"
      # cover: "{{ slots.name }}已关闭"
      cover_area: "{{ slots.area }}窗帘已关闭"
      cover_door: "{{ slots.area }}门已关闭"
      cover_window: "{{ slots.area }}窗户已关闭"
