language: fr-CA
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.name }} allumé"
      lights_area: "Lumières de {{ slots.area }} allumées"
      fans_area: "Ventilateurs de {{ slots.area }} activés"
      cover: "{{ slots.name }} ouvert"
      cover_area: "Ouvrants de {{ slots.area }} ouverts"
