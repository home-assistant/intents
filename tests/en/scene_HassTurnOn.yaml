language: en
tests:
  - sentences:
      - "activate party mode scene"
      - "turn party mode on"
      - "party mode on"
    intent:
      name: HassTurnOn
      slots:
        domain: scene
        name: "Party Mode"
    response: "Activated"
