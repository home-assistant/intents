language: en
tests:
  - sentences:
      - "good morning"
      - "it's morning"
      - "it is morning"
    intent:
      name: HassMorning
    response: "Good morning"
  - sentences:
      - "let's start the day"
    intent:
      name: HassMorning
    response: "Starting the day"
