language: en
tests:
  # power
  - sentences:
      - "What's the power consumption of the computer"
    intent:
      name: HassGetState
      slots:
        domain: "sensor"
        device_class: "power"
        name: "Computer"
    response: "Computer is 250 W"
