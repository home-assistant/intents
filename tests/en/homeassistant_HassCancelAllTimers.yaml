---
language: en
tests:
  - sentences:
      - "cancel all timers"
      - "stop all my timers"
      - "stop all the timers"
    intent:
      name: HassCancelAllTimers
    response: 3 timers cancelled

  - sentences:
      - "cancel all kitchen timers"
      - "stop all timers in kitchen"
      - "cancel all timers in the kitchen"
    intent:
      name: HassCancelAllTimers
      slots:
        area: Kitchen
    response: 1 timer cancelled
