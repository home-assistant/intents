language: fr
tests:
  - sentences:
      - "Lance le mode discret"
      - "Exécute le script mode discret"
      - "mode discret"
    intent:
      name: HassTurnOn
      slots:
        domain: script
        name: "Mode discret"
    response: "Script démarré"
