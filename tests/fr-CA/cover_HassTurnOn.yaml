language: fr-CA
tests:
- sentences:
  - ouvre la porte du garage
  intent:
    name: HassTurnOn
    slots:
      device_class: garage
      domain: cover
- sentences:
  - ouvre les stores de la cuisine
  intent:
    name: HassTurnOn
    slots:
      area: cuisine
      device_class:
      - blind
      - curtain
      - shutter
      domain: cover
