language: it
tests:
  - sentences:
      - "cancella tutti i timer"
      - "ferma tutti i timer"
      - "interrompi i miei timer"
    intent:
      name: HassCancelAllTimers
    response: Sono stati annullati 3 timer.

  - sentences:
      - "annullare tutti i timer in cucina"
      - "arresta i timer in cucina"
      - "fermare i timer in cucina"
    intent:
      name: HassCancelAllTimers
      slots:
        area: Cucina
    response: È stato annullato 1 timer in cucina.
